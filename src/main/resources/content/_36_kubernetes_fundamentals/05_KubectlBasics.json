{
  "theory": [
    {
      "type": "INTRO",
      "heading": "Jedno narzędzie CLI do WSZYSTKICH obiektów poznanych w lekcjach 1-4",
      "body": "Lekcje 1-4 pokazały CZTERY obiekty YAML (`Deployment`, `Pod`, `Service`, `ConfigMap`/`Secret`) — ta lekcja pokazuje `kubectl`, narzędzie CLI, PRZEZ KTÓRE WYSYŁASZ te definicje do klastra i SPRAWDZASZ ich rzeczywisty stan. To BEZPOŚREDNI odpowiednik `docker compose` z `_35/Lesson6` — TAM jedno narzędzie zarządzało CAŁYM stosem na jednym hoście, TU jedno narzędzie zarządza obiektami rozproszonymi na CAŁYM klastrze.",
      "code": null
    },
    {
      "type": "DEFINITION",
      "heading": "`kubectl` — jedyny punkt komunikacji z API klastra Kubernetesa",
      "body": "`kubectl` to narzędzie wiersza poleceń, które KOMUNIKUJE SIĘ z API klastra Kubernetesa — WSZYSTKO, co robisz z klastrem (wysyłanie definicji `Deployment`, sprawdzanie stanu Podów, przeglądanie logów), PRZECHODZI przez to JEDNO narzędzie. Podstawowe polecenia dzielą się na DWIE kategorie: `apply` (WYSYŁA definicję OBIEKTU do klastra) i `get`/`describe`/`logs` (ODCZYTUJĄ aktualny stan).",
      "code": null
    },
    {
      "type": "ANALOGY",
      "heading": "Pilot zdalnego sterowania CAŁYM klastrem, nie jednym urządzeniem",
      "body": "`docker compose` (`_35/Lesson6`) był jak pilot uniwersalny do URZĄDZEŃ na JEDNEJ scenie (jednym hoście) — `kubectl` jest jak pilot komunikujący się z CENTRALNYM SYSTEMEM zarządzającym CAŁYM budynkiem o WIELU piętrach (węzłach) naraz — NIE łączysz się BEZPOŚREDNIO z KONKRETNYM piętrem/urządzeniem, tylko WYSYŁASZ polecenie do CENTRALI (API klastra), a TA sama decyduje, które piętro/urządzenie faktycznie wykona zadanie.",
      "code": null
    },
    {
      "type": "VISUAL_EXAMPLE",
      "heading": "Cykl pracy: `apply` wysyła, `get`/`describe`/`logs` sprawdzają",
      "body": "kubectl apply -f deployment.yaml     <- WYSYLA definicje Deploymentu DO klastra\n         |\n         v\nKubernetes TWORZY/AKTUALIZUJE Pody wedlug tej definicji (mechanizm z lekcji 1-2)\n         |\n         v\nkubectl get pods                       <- SPRAWDZASZ, ILE Podow dziala i w jakim stanie\nkubectl describe pod tasks-api-a1b2c   <- SZCZEGOLY JEDNEGO konkretnego Poda (zdarzenia, bledy)\nkubectl logs tasks-api-a1b2c            <- LOGI z JEDNEGO konkretnego Poda (jak docker logs, _34/Lesson4)",
      "code": null
    },
    {
      "type": "CODE_BASIC",
      "heading": "Wysyłanie definicji obiektu do klastra",
      "body": "`kubectl apply -f <plik>` DZIAŁA analogicznie do `docker compose up` (`_35/Lesson6`) — WYSYŁA definicję z pliku YAML do klastra, TWORZĄC obiekt, JEŚLI nie istnieje, albo AKTUALIZUJĄC go, JEŚLI już istnieje (np. zmiana `image:` uruchamia rolling update z lekcji 2).",
      "code": "kubectl apply -f deployment.yaml    # wysyla/aktualizuje Deployment z pliku\nkubectl apply -f service.yaml       # wysyla/aktualizuje Service z pliku\nkubectl apply -f configmap.yaml     # wysyla/aktualizuje ConfigMap z pliku"
    },
    {
      "type": "CODE_PRACTICAL",
      "heading": "Podstawowe polecenia diagnostyczne",
      "body": "`get` daje SZYBKI przegląd, `describe` daje SZCZEGÓŁY (przydatne, gdy Pod NIE startuje poprawnie), `logs` daje treść logów aplikacji (jak `docker logs`, `_34/Lesson4`, TERAZ dla KONKRETNEGO Poda w klastrze).",
      "code": "kubectl get pods                        # lista Podow + status (Running/Pending/CrashLoopBackOff)\nkubectl get pods -o wide                 # jak wyzej, + wezel, na ktorym dziala kazdy Pod\nkubectl describe pod tasks-api-a1b2c     # szczegoly: zdarzenia, przyczyny bledow, zasoby\nkubectl logs tasks-api-a1b2c              # logi z GLOWNEGO kontenera tego Poda\nkubectl logs tasks-api-a1b2c -c log-shipper   # logi z KONKRETNEGO kontenera (Pod z lekcji 2 - sidecar)"
    },
    {
      "type": "STEP_BY_STEP",
      "heading": "Diagnozowanie Poda, który NIE startuje poprawnie",
      "body": "1. `kubectl get pods` — ZAUWAŻASZ, że status JEDNEGO Poda to NIE `Running`, tylko np. `CrashLoopBackOff` (kontener ciągle się RESTARTUJE) albo `Pending` (Pod CZEKA na zasoby/węzeł).\n2. `kubectl describe pod <nazwa>` — SPRAWDZASZ sekcję 'Events' na DOLE wyniku — CZĘSTO ZAWIERA KONKRETNY powód (np. 'brak wystarczających zasobów pamięci na żadnym węźle', 'nie udało się pobrać obrazu').\n3. JEŚLI Pod faktycznie WYSTARTOWAŁ, ale APLIKACJA WEWNĄTRZ niego się WYWALA — `kubectl logs <nazwa>` pokazuje DOKŁADNIE to, co aplikacja WYPISAŁA przed awarią (stack trace wyjątku Javy, JAK w normalnym logu Spring Boota).\n4. Na PODSTAWIE tych dwóch źródeł (Events z `describe`, treść logu z `logs`) DIAGNOZUJESZ, czy problem jest po stronie KONFIGURACJI Kubernetesa (np. zły `ConfigMap`, brak zasobów) czy po stronie SAMEJ aplikacji (błąd w kodzie, zła zmienna środowiskowa).",
      "code": null
    },
    {
      "type": "NOTE",
      "heading": "`kubectl` odczytuje stan Z KLASTRA, nie z pliku YAML na dysku",
      "body": "WAŻNE rozróżnienie: `kubectl get`/`describe`/`logs` ZAWSZE pokazują AKTUALNY stan OBIEKTÓW rzeczywiście istniejących W KLASTRZE — NIE zawartość pliku YAML na Twoim dysku. Jeśli EDYTUJESZ plik `deployment.yaml` LOKALNIE, ale NIE uruchomisz `kubectl apply -f`, klaster W OGÓLE nie widzi TEJ zmiany — DOKŁADNIE ta sama pułapka, co edytowanie plików JSON treści lekcji BEZ uruchomienia `mvnw resources:resources` przed restartem backendu (znana z wcześniejszych sesji pracy nad TĄ platformą).",
      "code": null
    },
    {
      "type": "USAGE",
      "heading": "Codzienny, minimalny zestaw poleceń wystarczający programiście Java",
      "body": "Zgodnie z zakresem tego rozdziału (`_36/Lesson1/USAGE` — programista Java ZWYKLE NIE administruje klastrem), WYSTARCZAJĄCY, praktyczny zestaw to: `kubectl get pods` (co się dzieje), `kubectl logs <pod>` (dlaczego coś nie działa), `kubectl describe pod <pod>` (szczegóły błędu) oraz `kubectl apply -f` (wdrożenie własnej zmiany, jeśli w OGÓLE leży to w zakresie Twojej roli). Reszta poleceń `kubectl` (zarządzanie węzłami, politykami bezpieczeństwa, zaawansowaną siecią) leży w gestii zespołu DevOps/Platform.",
      "code": null
    },
    {
      "type": "PITFALL",
      "heading": "Edycja pliku YAML lokalnie i ZAKŁADANIE, że klaster 'już to widzi'",
      "body": "Najczęstsza pomyłka osoby dopiero zaczynającej z Kubernetesem: ZMIANA wartości w pliku `deployment.yaml` (np. nowego `image:`) i PRZEJŚCIE od razu do `kubectl get pods`, OCZEKUJĄC zobaczenia zaktualizowanych Podów — BEZ uprzedniego `kubectl apply -f`, klaster NIGDY nie dowiaduje się o TEJ zmianie, bo `kubectl get` czyta stan KLASTRA, nie pliku na dysku (blok NOTE).",
      "code": null
    },
    {
      "type": "CODE_WRONG",
      "heading": "Zmiana pliku LOKALNIE, bez wysłania jej do klastra",
      "body": "Programista zmienia `image:` w pliku i OD RAZU sprawdza `kubectl get pods`, oczekując NOWEGO obrazu — Pody WCIĄŻ pokazują STARY obraz, bo zmiana NIGDY nie dotarła do klastra.",
      "code": "# programista edytuje deployment.yaml LOKALNIE:\n#   image: tasks-api:1.4.0  ->  image: tasks-api:1.5.0\n\n# i OD RAZU sprawdza:\nkubectl get pods -o jsonpath='{.items[0].spec.containers[0].image}'\n\n# BLEDNIE: wciaz pokazuje 'tasks-api:1.4.0' - zmiana istnieje TYLKO\n# lokalnie na dysku, klaster NIGDY nie zostal o niej poinformowany"
    },
    {
      "type": "CODE_RIGHT",
      "heading": "`kubectl apply -f` PRZED sprawdzeniem stanu klastra",
      "body": "Krok `apply` MUSI poprzedzić sprawdzenie stanu — DOPIERO po nim `kubectl get` pokazuje ZAKTUALIZOWANY obraz.",
      "code": "# po edycji deployment.yaml (image: tasks-api:1.5.0):\nkubectl apply -f deployment.yaml    # POPRAWNIE: NAJPIERW wyslij zmiane do klastra\n\n# DOPIERO TERAZ sprawdzenie pokazuje aktualny stan:\nkubectl get pods -o jsonpath='{.items[0].spec.containers[0].image}'\n# POPRAWNIE: pokazuje juz 'tasks-api:1.5.0' po zakonczonym rolling update (lekcja 2)"
    },
    {
      "type": "API_REFERENCE",
      "heading": "Najczęściej używane polecenia `kubectl`",
      "body": "`apply -f <plik>` — wysyła/aktualizuje definicję obiektu. `get <typ> [-o wide]` — lista obiektów danego typu (np. `pods`, `deployments`, `services`). `describe <typ> <nazwa>` — szczegóły jednego obiektu, w tym zdarzenia diagnostyczne. `logs <pod> [-c <kontener>]` — logi z Poda (opcjonalnie konkretnego kontenera, gdy Pod ma ich więcej niż jeden, lekcja 2). `delete -f <plik>` — usunięcie obiektu opisanego w pliku.",
      "code": null
    },
    {
      "type": "WHEN_TO_USE",
      "heading": "`kubectl` jako narzędzie CODE REVIEW/debugowania, nie CODZIENNEGO developmentu",
      "body": "W TYPOWEJ pracy nad KODEM aplikacji Java, WCIĄŻ pracujesz LOKALNIE (uruchamiając np. Docker Compose, `_35`) — `kubectl` staje się PRZYDATNY DOPIERO, gdy Twój kod jest JUŻ wdrożony na środowisku testowym/produkcyjnym opartym o Kubernetesa, i CHCESZ sprawdzić, DLACZEGO coś tam nie działa (blok STEP_BY_STEP) — TO właśnie SEDNO minimalnego zakresu tego rozdziału.",
      "code": null
    },
    {
      "type": "SUMMARY",
      "heading": "Podsumowanie",
      "body": "`kubectl` to JEDYNY punkt komunikacji z API klastra Kubernetesa — `apply -f` WYSYŁA definicje obiektów (Deployment/Service/ConfigMap/Secret z lekcji 1-4) DO klastra, a `get`/`describe`/`logs` ODCZYTUJĄ ich rzeczywisty, AKTUALNY stan. KLUCZOWE do zapamiętania: `kubectl` ZAWSZE pokazuje stan KLASTRA, NIGDY zawartości pliku na dysku — zmiana pliku YAML BEZ `apply` jest, z perspektywy klastra, jakby NIGDY nie zaszła. Minimalny, praktyczny zestaw dla programisty Java: `get pods`, `logs`, `describe pod`, `apply -f`.",
      "code": null
    }
  ],
  "exercises": [
    {
      "prompt": "Wyjaśnij (jako String), dlaczego zmiana wartości 'image:' w pliku deployment.yaml na dysku, bez uruchomienia 'kubectl apply -f', nie powoduje żadnej zmiany w rzeczywiście działających Podach klastra.",
      "hint": "Przypomnij sobie blok note - kubectl get/describe/logs zawsze czyta stan Z klastra, nigdy z pliku na dysku.",
      "solution": "Kubernetes dowiaduje sie o KAZDEJ zmianie w definicji obiektu WYLACZNIE poprzez explicit wyslanie jej do API klastra, komenda 'kubectl apply -f' - to WLASNIE ten krok informuje klaster 'oto nowa, pozadana wersja tego obiektu'. Sama edycja pliku na lokalnym dysku NIE komunikuje sie z klastrem w zaden automatyczny sposob - dopoki 'apply' nie zostanie uruchomione, klaster w ogole NIE WIE, ze plik zostal zmieniony, i kontynuuje dzialanie wedlug OSTATNIEJ wersji, ktora faktycznie zostala mu wyslana."
    },
    {
      "prompt": "Opisz (jako String) krok po kroku, jakich poleceń kubectl użyjesz, żeby zdiagnozować, dlaczego Pod aplikacji ma status 'CrashLoopBackOff' (kontener ciągle się restartuje).",
      "hint": "Przypomnij sobie blok step_BY_step - najpierw sprawdzasz ogolny status, potem szczegoly zdarzen, na koncu tresc logow aplikacji.",
      "solution": "Najpierw 'kubectl get pods', zeby POTWIERDZIC status 'CrashLoopBackOff' i dokladna nazwe Poda. Nastepnie 'kubectl describe pod <nazwa>', zeby sprawdzic sekcje 'Events' na dole wyniku - CZESTO zawiera konkretny powod na poziomie samego Kubernetesa (np. problem z pobraniem obrazu, brak zasobow). Na koncu 'kubectl logs <nazwa>', zeby zobaczyc, CO DOKLADNIE aplikacja wypisala PRZED awaria (np. stack trace wyjatku Javy) - laczac informacje z 'describe' (perspektywa Kubernetesa) i 'logs' (perspektywa samej aplikacji) mozna ustalic, czy problem lezy w konfiguracji Kubernetesa, czy w samym kodzie aplikacji."
    },
    {
      "prompt": "Wyjasnij (jako String), dlaczego polecenie 'kubectl logs <pod> -c log-shipper' wymaga podania flagi '-c', podczas gdy 'kubectl logs <pod>' dla zwyklego, jednokontenerowego Poda tej flagi nie wymaga - odwolaj sie do koncepcji z lekcji 2.",
      "hint": "Przypomnij sobie wzorzec 'sidecar' z lekcji 2 - Pod moze zawierac wiecej niz jeden kontener, wiec 'logs' musi wiedziec, ktorego dokladnie dotyczy.",
      "solution": "Pod moze zawierac WIECEJ niz jeden kontener (wzorzec 'sidecar' z lekcji 2, np. glowny kontener aplikacji plus pomocniczy kontener zbierajacy logi) - gdy Pod ma tylko JEDEN kontener, 'kubectl logs <pod>' jednoznacznie wie, o KTORY kontener chodzi, wiec flaga '-c' nie jest potrzebna. Gdy Pod ma WIECEJ niz jeden kontener, samo podanie nazwy Poda jest NIEJEDNOZNACZNE - trzeba JAWNIE wskazac, KTOREGO z kontenerow logi maja zostac pokazane, stad flaga '-c' ze wskazana nazwa konkretnego kontenera (np. 'log-shipper')."
    }
  ],
  "quiz": [
    {
      "question": "Do czego dokładnie służy polecenie 'kubectl apply -f <plik>'?",
      "options": {
        "A": "Wyłącznie do odczytu aktualnego stanu obiektów w klastrze, bez żadnej zmiany",
        "B": "Do wysłania definicji obiektu z pliku yaml do klastra, tworząc go lub aktualizując, jeśli już istnieje",
        "C": "Do trwałego usunięcia wszystkich Podów w klastrze",
        "D": "Do wyświetlenia listy dostępnych węzłów w klastrze"
      },
      "correct": "B",
      "explanation": "apply -f wysyła (tworzy lub aktualizuje) definicję obiektu opisaną w pliku yaml do klastra - to główny sposób wprowadzania zmian."
    },
    {
      "question": "Co dokładnie pokazują polecenia kubectl get/describe/logs?",
      "options": {
        "A": "Zawartość pliku yaml zapisanego lokalnie na dysku użytkownika",
        "B": "Aktualny, rzeczywisty stan obiektów faktycznie istniejących w klastrze, niezależnie od tego, co jest zapisane w lokalnych plikach",
        "C": "Historię wszystkich zmian, jakie kiedykolwiek wprowadzono do klastra",
        "D": "Wyłącznie dane statystyczne dotyczące zużycia sieci"
      },
      "correct": "B",
      "explanation": "kubectl zawsze odzwierciedla rzeczywisty stan klastra, nigdy zawartość lokalnych plików - zmiana pliku bez apply nie jest przez klaster w ogóle widoczna."
    },
    {
      "question": "Dlaczego zmiana wartości image: w lokalnym pliku deployment.yaml, bez uruchomienia kubectl apply -f, nie wpływa na działające Pody?",
      "options": {
        "A": "Ponieważ Kubernetes automatycznie skanuje dysk co godzinę w poszukiwaniu zmian w plikach",
        "B": "Ponieważ klaster dowiaduje się o zmianach wyłącznie poprzez jawne wysłanie ich przez kubectl apply - sama edycja pliku na dysku nie komunikuje się z klastrem",
        "C": "Ponieważ pliki yaml są automatycznie synchronizowane z klastrem w czasie rzeczywistym przez wbudowany mechanizm",
        "D": "Ponieważ zmiana wartości image: nigdy nie wywołuje żadnej reakcji w Kubernetesie"
      },
      "correct": "B",
      "explanation": "Klaster otrzymuje informację o zmianach wyłącznie poprzez jawne wysłanie ich komendą apply - lokalna edycja pliku bez tego kroku jest z perspektywy klastra niewidoczna."
    },
    {
      "question": "Kiedy przy poleceniu kubectl logs konieczne jest podanie dodatkowej flagi -c z nazwą kontenera?",
      "options": {
        "A": "Zawsze, niezależnie od liczby kontenerów w Podzie",
        "B": "Gdy dany Pod zawiera więcej niż jeden kontener (np. wzorzec sidecar) i trzeba jednoznacznie wskazać, którego z nich logi mają zostać pokazane",
        "C": "Wyłącznie gdy Pod działa na więcej niż jednym węźle jednocześnie",
        "D": "Flaga -c nigdy nie jest wymagana w żadnym scenariuszu"
      },
      "correct": "B",
      "explanation": "Flaga -c jest potrzebna, gdy Pod zawiera więcej niż jeden kontener, ponieważ samo wskazanie nazwy Poda staje się wtedy niejednoznaczne co do tego, którego kontenera logi mają zostać pokazane."
    },
    {
      "question": "Zgodnie z zalecanym w tym rozdziale zakresem, jaki minimalny zestaw poleceń kubectl wystarcza programiście Java niezajmującemu się administracją klastra?",
      "options": {
        "A": "Pełny zestaw poleceń do zarządzania węzłami, politykami bezpieczeństwa i zaawansowaną siecią klastra",
        "B": "get pods, logs, describe pod oraz apply -f (jeśli wdrożenie leży w zakresie roli) - do sprawdzania stanu, diagnozowania błędów i wdrażania własnych zmian",
        "C": "Wyłącznie polecenie kubectl delete, ponieważ inne polecenia są przestarzałe",
        "D": "Programista Java w ogóle nie powinien mieć dostępu do polecenia kubectl"
      },
      "correct": "B",
      "explanation": "Zgodnie z ograniczonym, praktycznym zakresem tego rozdziału, te cztery polecenia pokrywają typowe potrzeby programisty - sprawdzenie stanu, diagnozowanie problemów i wdrożenie własnej zmiany."
    }
  ]
}
