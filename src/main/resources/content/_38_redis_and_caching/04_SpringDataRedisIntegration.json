{
  "theory": [
    {
      "type": "INTRO",
      "heading": "Od `redis-cli` (lekcja 3) do prawdziwego kodu Spring Boot",
      "body": "Lekcja 3 pokazała ręczne polecenia przez `redis-cli` — ta lekcja pokazuje, jak aplikacja Spring Boot łączy się z Redis programowo, wykorzystując bibliotekę `Spring Data Redis` — dokładnie ta sama relacja, co JDBC/Spring Data JPA (`_09_jdbc`, `_11_hibernate`) wobec ręcznych poleceń `psql`.",
      "code": null
    },
    {
      "type": "DEFINITION",
      "heading": "`RedisTemplate`/`@Cacheable` — dwa poziomy integracji Spring Data Redis",
      "body": "`RedisTemplate` to klasa pozwalająca na bezpośrednie wywoływanie poleceń Redis z kodu Javy (odpowiednik `redis-cli`, ale programowy) — `@Cacheable` to adnotacja wysokiego poziomu, pozwalająca na 'automatyczne' cache'owanie wyniku metody, bez ręcznego wywoływania `GET`/`SET` — Spring sam decyduje, kiedy sprawdzić cache, a kiedy wykonać metodę.",
      "code": null
    },
    {
      "type": "ANALOGY",
      "heading": "`RedisTemplate` jak JDBC, `@Cacheable` jak Spring Data JPA",
      "body": "`RedisTemplate` jest jak surowe JDBC (`_09_jdbc`) — masz pełną kontrolę, ale musisz sam napisać każde wywołanie `GET`/`SET`. `@Cacheable` jest jak Spring Data JPA (`_11_hibernate`) — 'ukrywa' szczegóły za prostą adnotacją, Spring sam generuje potrzebny kod 'pod maską' — mniej kontroli, ale znacznie mniej kodu do napisania dla typowego przypadku 'sprawdź cache, jeśli puste — wykonaj metodę i zapisz wynik'.",
      "code": null
    },
    {
      "type": "VISUAL_EXAMPLE",
      "heading": "Co `@Cacheable` robi 'pod maską'",
      "body": "public class ZadaniaService {\n    @Cacheable(value = \"zadania\", key = \"#id\")\n    public Zadanie znajdzZadanie(Long id) {\n        return repository.findById(id).orElseThrow();  // kosztowne zapytanie do bazy danych\n    }\n}\n\nWywolanie znajdzZadanie(42L) pierwszy raz:\n  Spring sprawdza Redis pod kluczem \"zadania::42\" - pusto (cache miss)\n  Spring wykonuje metode (zapytanie do bazy) - zwraca Zadanie\n  Spring zapisuje wynik w Redis pod kluczem \"zadania::42\"\n\nWywolanie znajdzZadanie(42L) drugi raz:\n  Spring sprawdza Redis - znajduje wartosc (cache hit)\n  Spring zwraca wartosc bezposrednio z Redis - metoda w ogole nie jest wykonywana,\n  baza danych nie jest odpytywana ponownie",
      "code": null
    },
    {
      "type": "CODE_BASIC",
      "heading": "Konfiguracja połączenia z Redis w `application.properties`",
      "body": "Podobnie do konfiguracji JDBC (`_09_jdbc`), Spring Boot potrzebuje adresu i portu Redis — domyślnie `localhost:6379`, dokładnie jak uruchomiłeś w lekcji 3.",
      "code": "# application.properties\nspring.data.redis.host=localhost\nspring.data.redis.port=6379\nspring.cache.type=redis"
    },
    {
      "type": "CODE_PRACTICAL",
      "heading": "`@Cacheable`, `@CachePut`, `@CacheEvict` — pełny cykl cache'owania metody",
      "body": "`@Cacheable` sprawdza cache przed wykonaniem metody — `@CachePut` zawsze wykonuje metodę, a wynik zapisuje do cache (do aktualizacji istniejącego wpisu) — `@CacheEvict` usuwa wpis z cache (gdy dana staje się nieaktualna — więcej w lekcji 5).",
      "code": "@Service\npublic class ZadaniaService {\n\n    @Cacheable(value = \"zadania\", key = \"#id\")\n    public Zadanie znajdzZadanie(Long id) {\n        return repository.findById(id).orElseThrow();\n    }\n\n    @CachePut(value = \"zadania\", key = \"#zadanie.id\")\n    public Zadanie zaktualizujZadanie(Zadanie zadanie) {\n        return repository.save(zadanie);   // ZAWSZE zapisuje do bazy I aktualizuje cache\n    }\n\n    @CacheEvict(value = \"zadania\", key = \"#id\")\n    public void usunZadanie(Long id) {\n        repository.deleteById(id);          // USUWA wpis z cache PO usunieciu z bazy\n    }\n}"
    },
    {
      "type": "STEP_BY_STEP",
      "heading": "Jak `@Cacheable` wykorzystuje mechanizm Spring AOP (Aspect-Oriented Programming)",
      "body": "1. Spring, przy starcie aplikacji, tworzy proxy (pośrednik) wokół klasy `ZadaniaService` — wywołania metod nie trafiają bezpośrednio do twojego kodu, tylko najpierw do tego proxy.\n2. Gdy ktoś wywołuje `znajdzZadanie(42L)`, proxy przechwytuje to wywołanie, zanim dotrze do właściwej metody.\n3. Proxy sprawdza Redis pod kluczem wygenerowanym na podstawie `value`/`key` z adnotacji (`\"zadania::42\"`, blok visual_example).\n4. Jeśli wartość istnieje w cache, proxy zwraca ją bezpośrednio — twoja metoda `znajdzZadanie` nigdy nie jest faktycznie wykonana.\n5. Jeśli wartości nie ma, proxy przepuszcza wywołanie DO prawdziwej metody, odbiera jej wynik, zapisuje go w Redis, i dopiero potem zwraca DO wywołującego kodu.",
      "code": null
    },
    {
      "type": "NOTE",
      "heading": "`@Cacheable` nie działa przy wywołaniu 'z wewnątrz' tej samej klasy",
      "body": "Ponieważ `@Cacheable` opiera się na mechanizmie proxy (blok step_BY_step), wywołanie metody z adnotacją `@Cacheable` z wnętrza tej samej klasy (`this.znajdzZadanie(42L)`) omija proxy całkowicie — cache nigdy nie zostanie sprawdzony ani zapisany. To jest znane ograniczenie mechanizmu proxy Springa (dotyczy również `@Transactional`, `_21_spring_boot`) — wymaga wywołania metody przez wstrzyknięty bean, nie `this`.",
      "code": null
    },
    {
      "type": "USAGE",
      "heading": "Kiedy `@Cacheable` (wysoki poziom), kiedy `RedisTemplate` (niski poziom)",
      "body": "`@Cacheable` wystarcza dla typowego przypadku 'cache'uj wynik metody po jej argumencie' (blok code_practical) — pokrywa większość rzeczywistych potrzeb. `RedisTemplate` jest potrzebny, gdy potrzebujesz wykorzystać wyspecjalizowane struktury danych z lekcji 2 (`Sorted Set` dla rankingu, `List` dla kolejki) — czego `@Cacheable` nie robi (jest zaprojektowany wyłącznie do prostego cache'owania klucz-wartość wyniku metody).",
      "code": null
    },
    {
      "type": "PITFALL",
      "heading": "Cache'owanie metody modyfikującej dane za pomocą `@Cacheable` zamiast `@CachePut`",
      "body": "Przypadkowe użycie `@Cacheable` NA metodzie, która modyfikuje dane (np. `aktualizujZadanie`), spowoduje, że przy drugim wywołaniu z tym samym kluczem, Spring zwróci starą wartość z cache, zamiast wykonać metodę i zaktualizować dane — `@Cacheable` z założenia pomija wykonanie metody, gdy cache już istnieje, co jest przeciwieństwem zamierzonego zachowania dla metody aktualizującej.",
      "code": null
    },
    {
      "type": "CODE_WRONG",
      "heading": "`@Cacheable` na metodzie aktualizującej dane",
      "body": "Metoda aktualizująca zadanie, oznaczona błędnie jako `@Cacheable` — drugie wywołanie z tym samym ID zwróci starą wartość, pomijając rzeczywistą aktualizację.",
      "code": "@Cacheable(value = \"zadania\", key = \"#zadanie.id\")\npublic Zadanie zaktualizujZadanie(Zadanie zadanie) {\n    return repository.save(zadanie);\n}\n// BLEDNIE: przy DRUGIM wywolaniu z TYM SAMYM ID, Spring ZWROCI\n// STARA, ZACACHOWANA wartosc, POMIJAJAC wykonanie 'repository.save()' -\n// zadanie w bazie danych NIGDY nie zostanie zaktualizowane"
    },
    {
      "type": "CODE_RIGHT",
      "heading": "`@CachePut` na metodzie aktualizującej — zawsze wykonuje, zawsze aktualizuje cache",
      "body": "`@CachePut` zawsze wykonuje metodę (nigdy jej nie pomija), a wynik zapisuje do cache — właściwy wybór dla metod modyfikujących dane.",
      "code": "@CachePut(value = \"zadania\", key = \"#zadanie.id\")\npublic Zadanie zaktualizujZadanie(Zadanie zadanie) {\n    return repository.save(zadanie);\n}\n// POPRAWNIE: metoda JEST WYKONYWANA za KAZDYM razem (rzeczywista\n// aktualizacja w bazie danych), a WYNIK NADPISUJE poprzednia wartosc w cache"
    },
    {
      "type": "API_REFERENCE",
      "heading": "Kluczowe adnotacje Spring Cache",
      "body": "`@EnableCaching` — włączenie mechanizmu cache'owania w konfiguracji aplikacji (wymagane, zazwyczaj na klasie głównej). `@Cacheable(value, key)` — sprawdza cache przed wykonaniem metody. `@CachePut(value, key)` — zawsze wykonuje metodę, aktualizuje cache. `@CacheEvict(value, key)` — usuwa wpis z cache. `RedisTemplate<K, V>` — niskopoziomowy dostęp do wszystkich poleceń/struktur Redis z kodu Javy.",
      "code": null
    },
    {
      "type": "WHEN_TO_USE",
      "heading": "Zacznij od `@Cacheable`, sięgnij po `RedisTemplate` tylko gdy jest to konieczne",
      "body": "Analogicznie do zalecenia 'zacznij od Spring Data JPA, sięgnij po surowe JDBC tylko gdy trzeba' (`_11_hibernate`), `@Cacheable`/`@CachePut`/`@CacheEvict` pokrywają większość przypadków cache'owania wyników metod prostym, deklaratywnym kodem — `RedisTemplate` wprowadź dopiero, gdy realnie potrzebujesz wyspecjalizowanych struktur danych (lekcja 2), których proste adnotacje nie obsługują.",
      "code": null
    },
    {
      "type": "SUMMARY",
      "heading": "Podsumowanie",
      "body": "Spring Data Redis oferuje dwa poziomy integracji: `RedisTemplate` (bezpośredni dostęp do wszystkich poleceń Redis, analogiczny do surowego JDBC) i `@Cacheable`/`@CachePut`/`@CacheEvict` (deklaratywne cache'owanie wyniku metody, analogiczne do Spring Data JPA). `@Cacheable` sprawdza cache przed wykonaniem metody i pomija ją, gdy wartość już istnieje — dlatego nigdy nie powinien być użyty na metodzie modyfikującej dane (tam potrzebny jest `@CachePut`). Mechanizm opiera się na proxy Springa — wywołanie metody `this.metoda()` z wnętrza tej samej klasy omija cały mechanizm cache'owania.",
      "code": null
    }
  ],
  "exercises": [
    {
      "prompt": "Wyjaśnij (jako String), dlaczego uzycie adnotacji @Cacheable na metodzie, ktora aktualizuje dane w bazie (np. zapisuje zmieniony obiekt), jest bledem prowadzacym do powaznej niespojnosci danych.",
      "hint": "Przypomnij sobie blok code_wrong - @Cacheable Z zalozenia pomija wykonanie metody, gdy klucz juz istnieje w cache.",
      "solution": "@Cacheable dziala na zasadzie: 'sprawdz cache PRZED wykonaniem metody - jesli wartosc JUZ TAM jest, ZWROC ja BEZPOSREDNIO, POMIJAJAC wykonanie metody'. Dla METODY AKTUALIZUJACEJ dane (np. zapisujacej zmieniony obiekt do bazy), to zachowanie jest DOKLADNYM PRZECIWIENSTWEM zamierzonego efektu - PRZY DRUGIM wywolaniu z TYM SAMYM kluczem (np. tym samym ID obiektu), Spring ZWROCI STARA, ZACACHOWANA wartosc, CALKOWICIE POMIJAJAC faktyczne wykonanie metody aktualizujacej - zmiana NIGDY nie trafi do bazy danych, mimo ze kod WYGLADA, jakby dzialal poprawnie."
    },
    {
      "prompt": "Wyjasnij (jako String), dlaczego wywolanie metody oznaczonej @Cacheable Z wnetrza tej samej klasy (np. this.znajdzZadanie(42L) wewnatrz innej metody tej samej klasy) calkowicie omija mechanizm cache'owania.",
      "hint": "Przypomnij sobie blok step_BY_step - @Cacheable dziala poprzez proxy, ktore przechwytuje wywolania Z zewnatrz klasy, nie wewnetrzne wywolania 'this'.",
      "solution": "Spring implementuje @Cacheable poprzez mechanizm PROXY - przy starcie aplikacji Spring TWORZY dodatkowy obiekt 'posrednik' WOKOL prawdziwej klasy serwisu, i to WLASNIE ten proxy PRZECHWYTUJE wywolania metod PRZYCHODZACE Z ZEWNATRZ (np. z innego serwisu, kontrolera), sprawdzajac PRZY OKAZJI cache. Wywolanie 'this.metoda()' Z WNETRZA TEJ SAMEJ klasy NIE PRZECHODZI przez ten proxy w OGOLE - jest to BEZPOSREDNIE wywolanie metody na oryginalnym obiekcie, z POMINIECIEM calego mechanizmu przechwytywania. W REZULTACIE, cache NIGDY nie zostanie sprawdzony ani zaktualizowany dla TAKIEGO wewnetrznego wywolania, mimo ze metoda formalnie MA adnotacje @Cacheable."
    },
    {
      "prompt": "Zaprojektuj (jako String) poprawna kombinacje adnotacji Spring Cache dla serwisu z trzema metodami: 'znajdzProdukt(id)' (odczyt), 'zaktualizujCene(produkt)' (aktualizacja), 'usunProdukt(id)' (usuniecie). Uzasadnij wybor kazdej adnotacji.",
      "hint": "Przypomnij sobie API_reference - kazda z trzech operacji (odczyt/aktualizacja/usuniecie) ma dedykowana adnotacje w Spring Cache.",
      "solution": "'znajdzProdukt(id)' powinno miec adnotacje '@Cacheable(value=\"produkty\", key=\"#id\")' - to metoda WYLACZNIE odczytujaca, wiec sprawdzenie cache PRZED wykonaniem (i pominiecie metody przy trafieniu) jest POZADANYM zachowaniem. 'zaktualizujCene(produkt)' powinno miec adnotacje '@CachePut(value=\"produkty\", key=\"#produkt.id\")' - metoda MUSI byc wykonana ZA KAZDYM razem (rzeczywista aktualizacja w bazie), a JEJ wynik powinien NADPISAC wartosc w cache, aby KOLEJNE odczyty zwracaly juz zaktualizowana cene. 'usunProdukt(id)' powinno miec adnotacje '@CacheEvict(value=\"produkty\", key=\"#id\")' - PO usunieciu produktu z bazy, odpowiadajacy mu wpis w cache MUSI zostac USUNIETY, zeby KOLEJNE proby odczytu nie zwracaly juz NIEISTNIEJACEGO produktu z przestarzalego cache."
    }
  ],
  "quiz": [
    {
      "question": "Czym różni się RedisTemplate od adnotacji @Cacheable pod względem poziomu abstrakcji?",
      "options": {
        "A": "To dokładnie ta sama funkcjonalność pod dwiema różnymi nazwami",
        "B": "RedisTemplate daje bezpośredni, niskopoziomowy dostęp do wszystkich poleceń Redis, podczas gdy @Cacheable to wysokopoziomowa adnotacja automatyzująca typowe cache'owanie wyniku metody",
        "C": "RedisTemplate działa wyłącznie z bazami relacyjnymi, a @Cacheable wyłącznie z Redis",
        "D": "@Cacheable jest przestarzałą, niezalecaną już funkcją Springa"
      },
      "correct": "B",
      "explanation": "RedisTemplate to niskopoziomowy odpowiednik surowego jdbc, a @Cacheable to wysokopoziomowa adnotacja analogiczna do Spring Data JPA."
    },
    {
      "question": "Co dokładnie robi adnotacja @Cacheable przy wywołaniu oznaczonej nią metody?",
      "options": {
        "A": "Zawsze wykonuje metodę, niezależnie od tego, czy wartość jest już w cache",
        "B": "Sprawdza cache przed wykonaniem metody - jeśli wartość już tam jest, zwraca ją bezpośrednio, pomijając faktyczne wykonanie metody",
        "C": "Automatycznie usuwa wszystkie wcześniej zapisane wartości cache przy każdym wywołaniu",
        "D": "Wymaga ręcznego wywołania metody GET z Redis wewnątrz ciała metody"
      },
      "correct": "B",
      "explanation": "@Cacheable sprawdza cache przed wykonaniem metody i pomija jej wykonanie, jeśli odpowiadająca wartość już istnieje w cache."
    },
    {
      "question": "Dlaczego użycie @Cacheable zamiast @CachePut na metodzie aktualizującej dane jest błędem?",
      "options": {
        "A": "Nie jest błędem - obie adnotacje działają identycznie w każdym scenariuszu",
        "B": "Ponieważ @Cacheable pomija wykonanie metody, gdy wartość już istnieje w cache, co oznacza, że rzeczywista aktualizacja danych nigdy nie nastąpi przy powtórnym wywołaniu z tym samym kluczem",
        "C": "Ponieważ @Cacheable technicznie nie obsługuje metod z parametrami",
        "D": "Ponieważ @Cacheable automatycznie usuwa dane z bazy danych po każdym wywołaniu"
      },
      "correct": "B",
      "explanation": "@Cacheable z założenia omija wykonanie metody przy trafieniu w cache - dla metody aktualizującej dane oznacza to pominięcie faktycznej aktualizacji."
    },
    {
      "question": "Dlaczego wywołanie metody oznaczonej @Cacheable z wnętrza tej samej klasy (przez 'this') nie korzysta z mechanizmu cache'owania?",
      "options": {
        "A": "Ponieważ Spring technicznie zabrania takich wywołań i zgłasza błąd kompilacji",
        "B": "Ponieważ @Cacheable działa poprzez proxy przechwytujący wywołania z zewnątrz klasy - wywołanie 'this.metoda()' omija ten proxy całkowicie",
        "C": "Ponieważ cache Redis nie obsługuje wywołań wewnętrznych klas Java",
        "D": "Ponieważ adnotacja @Cacheable wymaga jawnego podania adresu IP wywołującego kodu"
      },
      "correct": "B",
      "explanation": "Mechanizm proxy Springa przechwytuje wywołania z zewnątrz klasy - wewnętrzne wywołanie 'this' nie przechodzi przez ten proxy, więc cache nie jest w ogóle sprawdzany."
    },
    {
      "question": "Kiedy warto sięgnąć po RedisTemplate zamiast prostych adnotacji @Cacheable/@CachePut/@CacheEvict?",
      "options": {
        "A": "Zawsze - RedisTemplate powinien być domyślnym wyborem dla każdego przypadku cache'owania",
        "B": "Gdy potrzebne jest wykorzystanie wyspecjalizowanych struktur danych Redis (jak Sorted Set dla rankingu), których proste adnotacje nie obsługują",
        "C": "Nigdy - RedisTemplate jest przestarzałym mechanizmem zastąpionym całkowicie przez adnotacje",
        "D": "Wyłącznie gdy aplikacja nie korzysta ze Spring Boot"
      },
      "correct": "B",
      "explanation": "Adnotacje pokrywają typowe przypadki cache'owania wyniku metody, ale RedisTemplate jest potrzebny do wykorzystania bardziej zaawansowanych struktur danych Redis wykraczających poza prosty cache klucz-wartość."
    }
  ]
}
