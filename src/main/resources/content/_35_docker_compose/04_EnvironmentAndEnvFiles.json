{
  "theory": [
    {
      "type": "INTRO",
      "heading": "Hasła i klucze API NIE należą do pliku Compose",
      "body": "Do tej pory zmienne środowiskowe (`environment:`, lekcje 1-3) wpisywaliśmy WPROST w pliku `docker-compose.yml` — WYGODNE do nauki, ale w PRAKTYCE ryzykowne: plik Compose TRAFIA do repozytorium Git (`_33`, kontrola wersji), a razem z nim KAŻDY sekret zapisany wprost w `environment:`. Ta lekcja pokazuje MECHANIZM podstawiania zmiennych (`${...}`) oraz plik `.env` — sposób na ODDZIELENIE tego, CO uruchamiasz (plik Compose, bezpieczny do commitowania), od tego, Z JAKIMI konkretnie wartościami (plik `.env`, NIGDY nie commitowany).",
      "code": null
    },
    {
      "type": "DEFINITION",
      "heading": "Plik `.env` — domyślne źródło podstawień dla `${ZMIENNA}`",
      "body": "Compose, PRZED przetworzeniem pliku `docker-compose.yml`, AUTOMATYCZNIE szuka pliku o DOKŁADNEJ nazwie `.env` w TYM SAMYM katalogu — jeśli istnieje, KAŻDE wystąpienie składni `${NAZWA}` w pliku Compose jest PODSTAWIANE wartością zmiennej `NAZWA` z tego pliku, ZANIM Compose w ogóle zacznie interpretować resztę YAML-a. To DZIEJE SIĘ NIEZALEŻNIE od właściwości `env_file:` (lekcja opisana niżej) — `.env` to ODDZIELNY, dodatkowy mechanizm PODSTAWIANIA WARTOŚCI W SAMYM PLIKU COMPOSE, nie przekazywania zmiennych DO kontenera.",
      "code": null
    },
    {
      "type": "ANALOGY",
      "heading": "Szablon umowy z polami do wypełnienia",
      "body": "Plik `docker-compose.yml` ze składnią `${HASLO_BAZY}` jest jak SZABLON umowy z polem '[__________]' do wypełnienia — sam SZABLON jest OGÓLNY i BEZPIECZNY do pokazania komukolwiek (nie zawiera niczyich prawdziwych danych). Plik `.env` jest jak ODDZIELNA kartka z KONKRETNYMI danymi do wpisania w te pola — TĘ kartkę trzymasz w SEJFIE (poza repozytorium), NIE dołączasz jej do skoroszytu z szablonami, które pokazujesz wszystkim. Każdy nowy pracownik dostaje TEN SAM szablon, ale WYPEŁNIA go WŁASNYMI, indywidualnymi danymi.",
      "code": null
    },
    {
      "type": "VISUAL_EXAMPLE",
      "heading": "Podstawienie `${...}` krok po kroku",
      "body": "Plik '.env':\nDB_PASSWORD=tajne_haslo_produkcyjne\nDB_NAME=javaquest\n\nPlik 'docker-compose.yml' (PRZED podstawieniem):\nservices:\n  baza:\n    image: postgres:16\n    environment:\n      POSTGRES_PASSWORD: ${DB_PASSWORD}\n      POSTGRES_DB: ${DB_NAME}\n\nCo Compose FAKTYCZNIE widzi PO podstawieniu (zanim zacznie cokolwiek uruchamiac):\nservices:\n  baza:\n    image: postgres:16\n    environment:\n      POSTGRES_PASSWORD: tajne_haslo_produkcyjne\n      POSTGRES_DB: javaquest",
      "code": null
    },
    {
      "type": "CODE_BASIC",
      "heading": "Wartość domyślna i wymagane zmienne w składni `${...}`",
      "body": "Składnia `${ZMIENNA:-wartosc_domyslna}` podstawia `wartosc_domyslna`, GDY zmienna NIE jest ustawiona (albo jest PUSTA) — `${ZMIENNA:?komunikat_bledu}` sprawia, że Compose ZATRZYMUJE SIĘ z BŁĘDEM, jeśli zmienna nie jest ustawiona (przydatne dla sekretów, które MUSZĄ być podane, bez cichego 'brakującego' zachowania).",
      "code": "services:\n  baza:\n    image: postgres:16\n    environment:\n      POSTGRES_DB: ${DB_NAME:-javaquest}              # domyslnie \"javaquest\", jesli brak w .env\n      POSTGRES_PASSWORD: ${DB_PASSWORD:?Brak DB_PASSWORD w pliku .env!}  # WYMAGANE, inaczej blad"
    },
    {
      "type": "CODE_PRACTICAL",
      "heading": "`env_file:` — wstrzykiwanie CAŁEGO pliku zmiennych DO kontenera",
      "body": "Właściwość `env_file:` (na poziomie SERWISU, nie do mylenia z plikiem `.env` z bloku DEFINITION) wczytuje WSKAZANY plik i wstrzykuje WSZYSTKIE jego linie jako zmienne środowiskowe WEWNĄTRZ kontenera — jak dziesiątki linii `environment:` naraz, bez przepisywania ich w YAML.",
      "code": "# plik: tasks-api.env (NIE commitowany - w .gitignore)\nSPRING_DATASOURCE_URL=jdbc:postgresql://baza:5432/javaquest\nSPRING_DATASOURCE_USERNAME=javaquest_user\nSPRING_DATASOURCE_PASSWORD=tajne_haslo_produkcyjne\nJWT_SECRET=bardzo-dlugi-losowy-sekret-produkcyjny\n\n# docker-compose.yml\nservices:\n  tasks-api:\n    build: .\n    env_file:\n      - tasks-api.env"
    },
    {
      "type": "STEP_BY_STEP",
      "heading": "Kolejność pierwszeństwa, gdy TA SAMA zmienna jest ustawiona w kilku miejscach",
      "body": "1. Compose NAJPIERW wczytuje plik `.env` (jeśli istnieje) — TYLKO do podstawień `${...}` w SAMYM pliku Compose (blok DEFINITION).\n2. Jeśli serwis ma `env_file:`, Compose wstrzykuje WSZYSTKIE zmienne z WSKAZANEGO pliku BEZPOŚREDNIO do środowiska kontenera.\n3. Jeśli TA SAMA zmienna pojawia się RÓWNIEŻ w sekcji `environment:` TEGO serwisu, wartość z `environment:` WYGRYWA — NADPISUJE tę z `env_file:` (bardziej SZCZEGÓŁOWE ustawienie ma PIERWSZEŃSTWO nad bardziej OGÓLNYM).\n4. Zmienna eksportowana w POWŁOCE, z KTÓREJ uruchamiasz `docker compose` (np. `export DB_PASSWORD=xyz` przed `docker compose up`), MA NAJWYŻSZY priorytet ze wszystkich — nadpisuje NAWET wartość z pliku `.env` przy podstawieniach `${...}`.",
      "code": null
    },
    {
      "type": "NOTE",
      "heading": "`.env` a `env_file:` — dwa RÓŻNE mechanizmy o podobnej nazwie",
      "body": "To NAJCZĘSTSZE źródło pomyłki w tej lekcji: plik `.env` (bez podanej nazwy we właściwości, automatycznie wykrywany) służy WYŁĄCZNIE do podstawień `${...}` W SAMYM pliku Compose, ZANIM cokolwiek zostanie uruchomione — NIE trafia automatycznie DO środowiska kontenera. Właściwość `env_file:` (jawnie wskazana, DOWOLNA nazwa pliku) wstrzykuje zmienne BEZPOŚREDNIO do kontenera, ale NIE wpływa na podstawienia `${...}` gdzie indziej w pliku Compose. To DWA NIEZALEŻNE mechanizmy, mimo że oba operują na plikach `KLUCZ=wartosc`.",
      "code": null
    },
    {
      "type": "USAGE",
      "heading": "Praktyczny podział: `.env` dla wspólnych ustawień, osobne pliki dla sekretów per-serwis",
      "body": "Typowy, DOBRY wzorzec: plik `.env` w katalogu głównym trzyma wartości UŻYWANE do podstawień w SAMYM pliku Compose (np. numer wersji obrazu, nazwa projektu) — WSPÓLNE dla całego stosu. Osobne pliki `*.env` per serwis (`tasks-api.env`, `baza.env`), wskazywane przez `env_file:`, trzymają WŁAŚCIWE sekrety danego serwisu. WSZYSTKIE takie pliki (zarówno `.env`, jak i `*.env`) TRAFIAJĄ do `.gitignore` — commitowany jest TYLKO plik-przykład (`np. .env.example`) z NAZWAMI zmiennych i PUSTYMI/przykładowymi wartościami, jako dokumentacja tego, co trzeba wypełnić.",
      "code": null
    },
    {
      "type": "PITFALL",
      "heading": "Commitowanie pliku `.env` z prawdziwymi sekretami",
      "body": "Najczęstszy, POWAŻNY błąd: potraktowanie pliku `.env` jak KAŻDEGO innego pliku konfiguracyjnego i dodanie go do repozytorium przez `git add .` (`_33`, ryzyko commitowania WSZYSTKIEGO bez przeglądu) — sekrety TRAFIAJĄ wtedy do HISTORII Gita, gdzie zostają NA ZAWSZE (nawet po późniejszym usunięciu pliku, stara wersja WCIĄŻ jest dostępna w historii commitów, `_33/Lesson historia`).",
      "code": null
    },
    {
      "type": "CODE_WRONG",
      "heading": "Prawdziwe hasło produkcyjne trafia do repozytorium",
      "body": "Plik `.env` z prawdziwymi wartościami, BEZ wpisu w `.gitignore` — jeden `git add .` (`_33`) i sekret trafia do repozytorium, widoczny dla KAŻDEGO, kto ma do niego dostęp, NA ZAWSZE w historii.",
      "code": "# plik: .env  (BRAK wpisu \".env\" w .gitignore!)\nDB_PASSWORD=Prawdziwe_Haslo_Produkcyjne_2024!\nJWT_SECRET=a8f5f167f44f4964e6c998dee827110c\n\n# BLEDNIE: 'git add .' zacommituje TEN plik razem z reszta zmian,\n# bo nic go nie ignoruje"
    },
    {
      "type": "CODE_RIGHT",
      "heading": "`.gitignore` blokuje sekrety, `.env.example` dokumentuje strukturę",
      "body": "Prawdziwy plik `.env` NIGDY nie trafia do repozytorium — commitowany jest TYLKO wzorcowy plik z nazwami zmiennych, bez rzeczywistych wartości.",
      "code": "# plik: .gitignore\n.env\n*.env\n!.env.example\n\n# plik: .env.example  (TEN plik JEST commitowany - dokumentacja)\nDB_PASSWORD=zmien_na_wlasne_haslo\nJWT_SECRET=zmien_na_wlasny_losowy_sekret\n\n# POPRAWNIE: kazdy nowy programista kopiuje '.env.example' do '.env'\n# i wypelnia WLASNYMI, lokalnymi wartosciami - '.env' nigdy nie opuszcza jego dysku"
    },
    {
      "type": "API_REFERENCE",
      "heading": "Składnia podstawień i wstrzykiwania zmiennych",
      "body": "`${ZMIENNA}` — podstawienie wartości z `.env`/środowiska powłoki w pliku Compose. `${ZMIENNA:-domyslna}` — z wartością domyślną. `${ZMIENNA:?blad}` — wymagane, inaczej błąd i zatrzymanie. `environment:` (na poziomie serwisu) — pojedyncze zmienne wprost do kontenera (lekcje 1-3). `env_file:` (na poziomie serwisu) — lista plików, z KTÓRYCH WSZYSTKIE linie trafiają jako zmienne do kontenera. Kolejność pierwszeństwa (rosnąco): `env_file:` < `environment:` < zmienna wyeksportowana w powłoce uruchamiającej `docker compose`.",
      "code": null
    },
    {
      "type": "WHEN_TO_USE",
      "heading": "Kiedy `environment:` wystarczy, kiedy sięgnąć po `.env`/`env_file:`",
      "body": "Proste, JAWNE wartości NIEBĘDĄCE sekretem (np. `SPRING_PROFILES_ACTIVE: docker`) mogą ZOSTAĆ wprost w `environment:` — czytelność wygrywa z niepotrzebną abstrakcją. W MOMENCIE, gdy wartość jest sekretem (hasło, klucz API, token) LUB gdy TA SAMA wartość ma być INNA w różnych środowiskach (dev/staging/prod) BEZ zmiany samego pliku Compose, przenieś ją do `.env`/`env_file:` — DOKŁADNIE ten moment jest granicą, po której 'wygoda wprost w YAML' zamienia się w 'ryzyko bezpieczeństwa i sztywność'.",
      "code": null
    },
    {
      "type": "SUMMARY",
      "heading": "Podsumowanie",
      "body": "Plik `.env` (automatycznie wykrywany) podstawia wartości pod składnię `${ZMIENNA}` W SAMYM pliku Compose, ZANIM cokolwiek zostanie uruchomione — to NIE to samo, co wstrzykiwanie zmiennych do kontenera. Właściwość `env_file:` (na poziomie serwisu) wstrzykuje CAŁY wskazany plik jako zmienne środowiskowe BEZPOŚREDNIO do kontenera. Przy konflikcie: `environment:` NADPISUJE `env_file:`, a zmienna wyeksportowana w powłoce nadpisuje WSZYSTKO. Prawdziwe sekrety NIGDY nie trafiają do repozytorium — `.gitignore` blokuje `.env`/`*.env`, a commitowany jest jedynie wzorcowy `.env.example` z samymi nazwami zmiennych.",
      "code": null
    }
  ],
  "exercises": [
    {
      "prompt": "Mając plik '.env' z zawartością 'tag=1.4.0' oraz fragment pliku Compose 'image: tasks-api:${tag}', napisz (jako String), co dokładnie zobaczy Compose po podstawieniu, zanim zacznie cokolwiek uruchamiać.",
      "hint": "Podstawienie '${tag}' zostaje zastapione wartoscia zmiennej tag z pliku '.env' - to dzieje sie zanim Compose zinterpretuje reszte yaml-a.",
      "solution": "Po podstawieniu Compose zobaczy: 'image: tasks-api:1.4.0' - fragment '${TAG}' zostal doslownie zastapiony wartoscia zmiennej TAG odczytana z pliku '.env' (czyli '1.4.0'), zanim Compose w ogole zaczal interpretowac reszte pliku YAML."
    },
    {
      "prompt": "Wyjaśnij (jako String) różnicę między plikiem `.env` (automatycznie wykrywanym) a właściwością `env_file:` w sekcji serwisu - czy plik '.env' automatycznie trafia jako zmienne środowiskowe do kontenera?",
      "hint": "Przypomnij sobie blok note - to sa dwa odrebne mechanizmy, mimo pozornego podobienstwa nazw.",
      "solution": "NIE - plik '.env' (bez jawnego wskazania we wlasciwosci, automatycznie wykrywany przez Compose) sluzy WYLACZNIE do podstawien skladni '${ZMIENNA}' w SAMYM pliku Compose, PRZED jego interpretacja - jego zawartosc NIE trafia automatycznie do srodowiska kontenera. Aby wstrzyknac zmienne BEZPOSREDNIO do kontenera, trzeba jawnie uzyc wlasciwosci 'env_file:' na poziomie serwisu, wskazujac KONKRETNY plik (moze to byc TEN SAM plik '.env' lub zupelnie inny) - to DRUGI, niezalezny mechanizm od automatycznego podstawiania '${...}'."
    },
    {
      "prompt": "Serwis ma 'env_file: [wspolne.env]' (zawierajacy 'spring_profiles_active=dev') oraz 'environment: [spring_profiles_active=docker]'. Napisz (jako String), ktora wartosc zmiennej spring_profiles_active zobaczy kontener po uruchomieniu, i dlaczego.",
      "hint": "Przypomnij sobie kolejnosc pierwszenstwa z bloku step_BY_step - 'environment:' jest bardziej szczegolowym ustawieniem niz 'env_file:'.",
      "solution": "Kontener zobaczy wartosc 'docker' (z sekcji 'environment:'), NIE 'dev' (z 'wspolne.env') - zgodnie z kolejnoscia pierwszenstwa Compose, wartosc ustawiona wprost w 'environment:' danego serwisu ZAWSZE NADPISUJE te sama zmienna wczytana z pliku wskazanego przez 'env_file:', poniewaz jest traktowana jako bardziej szczegolowe, swiadome ustawienie."
    },
    {
      "prompt": "Masz `.env` z `TAG=1.4.0` oraz serwis z `image: tasks-api:${TAG}` i `command: [\"sh\",\"-c\",\"echo $$TAG\"]`. Przewidź (jako String), jaki obraz zostanie użyty i co wypisze kontener, wyjaśniając różnicę między `${TAG}` a `$$TAG`.",
      "hint": ".env służy do podstawień w pliku Compose, ale NIE trafia automatycznie do kontenera. $$ to ucieczka - dosłowny znak $ przekazany do kontenera.",
      "solution": "Obraz: tasks-api:1.4.0 - ${TAG} zostało podstawione przez Compose z .env. Kontener wypisze pusty wiersz: $$TAG to dosłowne $TAG przekazane do powłoki w kontenerze, a TAG nie jest zmienną środowiskową kontenera (.env nie jest automatycznie wstrzykiwany). Aby kontener ją widział: environment: [TAG=${TAG}] albo env_file: [.env]."
    },
    {
      "prompt": "Serwis ma `env_file: [wspolne.env]` (zawiera `PROFILE=dev` i `ONLYFILE=x`) oraz `environment: {PROFILE: docker}`. Przewidź (jako String) wartości `PROFILE` i `ONLYFILE` w kontenerze i opisz pełną kolejność pierwszeństwa źródeł wartości zmiennych.",
      "hint": "environment ma pierwszeństwo przed env_file. ENV z obrazu jest najsłabsze.",
      "solution": "PROFILE=docker (environment wygrywa z env_file), ONLYFILE=x (pochodzi tylko z pliku). Kolejność od najsilniejszego: 1) docker compose run -e, 2) environment: w serwisie (z wartościami podstawionymi z powłoki/.env), 3) env_file:, 4) ENV w Dockerfile obrazu. Osobno: podstawienia ${...} w samym pliku biorą wartość z powłoki, a dopiero gdy jej brak - z pliku .env."
    },
    {
      "prompt": "Przygotuj (jako String) zestaw plików dla zespołu: `.env.example` (commitowany), `.env` (lokalny), wpisy `.gitignore` i `.dockerignore`, oraz fragment `compose.yaml`, który przerwie uruchomienie z czytelnym komunikatem, gdy ktoś zapomni ustawić hasła.",
      "hint": "${VAR:?komunikat}. .env.example bez prawdziwych wartości. Wyjątek !.env.example w .gitignore.",
      "solution": "# .env.example (commitowany)\nDB_PASSWORD=zmien_mnie\nJWT_SECRET=zmien_mnie_na_dlugi_losowy_ciag\nAPP_PORT=8080\n\n# .gitignore\n.env\n*.env\n!.env.example\n\n# .dockerignore\n.env*\n\n# compose.yaml\nservices:\n  baza:\n    environment:\n      POSTGRES_PASSWORD: ${DB_PASSWORD:?Skopiuj .env.example do .env i ustaw DB_PASSWORD}\n  api:\n    ports: [\"${APP_PORT:-8080}:8080\"]\n    environment:\n      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD:?Brak DB_PASSWORD}\n      JWT_SECRET: ${JWT_SECRET:?Brak JWT_SECRET}\n\nNowa osoba: cp .env.example .env, uzupełnia wartości, docker compose up -d."
    },
    {
      "prompt": "Pokaż (jako String), jak przekazać hasło do PostgreSQL przez mechanizm `secrets:` w Compose (plik montowany w `/run/secrets`), zamiast przez zmienną środowiskową, i wyjaśnij, dlaczego to lepsze. Jak aplikacja Spring Boot może odczytać taki sekret?",
      "hint": "Obraz postgres obsługuje POSTGRES_PASSWORD_FILE. Spring Boot potrafi importować pliki przez spring.config.import=configtree:.",
      "solution": "services:\n  baza:\n    image: postgres:16\n    environment:\n      POSTGRES_PASSWORD_FILE: /run/secrets/db_password\n    secrets: [db_password]\n  api:\n    build: .\n    environment:\n      SPRING_CONFIG_IMPORT: \"configtree:/run/secrets/\"\n    secrets: [db_password]\nsecrets:\n  db_password:\n    file: ./secrets/db_password.txt   # poza repozytorium\n\nSekret jest montowany jako plik tylko w kontenerach, które go deklarują - nie widać go w docker inspect (Config.Env), w środowisku procesów ani w logach wypisujących zmienne. Spring Boot z configtree czyta pliki z katalogu jako właściwości (nazwa pliku = nazwa właściwości), więc aplikacja może używać np. ${db_password}."
    },
    {
      "prompt": "Wyjaśnij (jako String), jak działa flaga `--env-file` dla `docker compose` (np. `docker compose --env-file .env.test up`) i czym różni się od `env_file:` w definicji serwisu. Pokaż, jak trzymać ustawienia dla trzech środowisk w osobnych plikach.",
      "hint": "--env-file zastępuje plik .env używany do podstawień ${...} w compose.yaml.",
      "solution": "docker compose --env-file .env.test up -d używa .env.test ZAMIAST .env do podstawień ${...} w pliku Compose (tagi obrazów, porty, nazwy baz). Nic nie trafia automatycznie do kontenerów - chyba że plik przekazuje wartości przez environment. env_file: w serwisie wstrzykuje zmienne z pliku DO kontenera. Wzorzec: .env.dev, .env.test, .env.prod z tymi samymi kluczami (TAG, APP_PORT, DB_NAME), wybierane flagą; sekrety - w osobnych plikach/sekretach na serwerze."
    },
    {
      "prompt": "Hasło zawiera znak `$` (np. `Pa$$word`). Wyjaśnij (jako String), co się stanie, gdy wpiszesz je wprost w `environment:` w compose.yaml, a co - gdy trafi przez plik `env_file`. Jak poprawnie je zapisać w pliku Compose?",
      "hint": "Compose interpoluje $ w pliku compose.yaml; $$ oznacza dosłowny $.",
      "solution": "W compose.yaml każde $ jest traktowane jako początek podstawienia: 'Pa$$word' zamieni się w 'Pa$word' ($$ -> $), a 'Pa$word' - w 'Pa' + wartość zmiennej 'word' (zwykle pusta, z ostrzeżeniem). Poprawnie w pliku Compose: każde dosłowne $ zapisać jako $$ (hasło Pa$$word -> Pa$$$$word). W env_file (i .env) wartości nie są interpolowane w ten sam sposób co compose.yaml - bezpieczniej trzymać tam takie hasła, a najlepiej w sekretach montowanych jako plik. docker compose config pokaże wartość po przetworzeniu."
    },
    {
      "prompt": "Opisz (jako String) procedurę naprawczą po tym, jak `.env` z prawdziwym hasłem do bazy produkcyjnej został wypchnięty na GitHub. Uszereguj kroki według ważności i wskaż, które z nich dotyczą Gita, a które - systemów poza nim.",
      "hint": "Najpierw unieważnienie sekretu, potem sprzątanie repozytorium i zapobieganie powtórce (lekcja 12 z _33).",
      "solution": "1) Natychmiast zmienić hasło do bazy (i inne sekrety z pliku) - od tej chwili wartość w historii jest bezużyteczna. Poza Gitem, najważniejsze. 2) Sprawdzić logi bazy/dostępu pod kątem użycia hasła. 3) git rm --cached .env, dopisać .env do .gitignore, commit, push. 4) Dodać .env.example i walidację ${VAR:?} w compose.yaml. 5) Opcjonalnie usunąć plik z historii (git filter-repo/BFG) + force push po uzgodnieniu z zespołem - kopie mogły już zostać pobrane, więc to nie zastępuje kroku 1. 6) Włączyć skanowanie sekretów (GitHub secret scanning, hook pre-commit)."
    }
  ],
  "quiz": [
    {
      "question": "Do czego dokładnie służy automatycznie wykrywany plik '.env' (bez jawnego wskazania we właściwości env_file:)?",
      "options": {
        "A": "Automatycznie wstrzykuje wszystkie swoje zmienne do środowiska każdego kontenera",
        "B": "Podstawia wartości pod składnię ${zmienna} w samym pliku Compose, zanim zostanie on zinterpretowany i cokolwiek uruchomione",
        "C": "Jest wymagany do uruchomienia jakiegokolwiek pliku Compose",
        "D": "Zastępuje całkowicie potrzebę używania sekcji environment:"
      },
      "correct": "B",
      "explanation": "Plik '.env' służy wyłącznie do podstawień składni ${...} w samym pliku Compose przed jego interpretacją - to inny mechanizm niż wstrzykiwanie zmiennych bezpośrednio do kontenera."
    },
    {
      "question": "Jaka jest kolejność pierwszeństwa, gdy ta sama zmienna środowiskowa jest ustawiona zarówno w env_file:, jak i w environment: tego samego serwisu?",
      "options": {
        "A": "env_file: zawsze wygrywa, niezależnie od kolejności w pliku",
        "B": "environment: nadpisuje wartość wczytaną z env_file:",
        "C": "Compose zgłasza błąd i odmawia uruchomienia przy jakimkolwiek konflikcie",
        "D": "Wartości są automatycznie łączone w jeden ciąg znaków"
      },
      "correct": "B",
      "explanation": "environment: jest bardziej szczegółowym ustawieniem i nadpisuje tę samą zmienną wczytaną z pliku wskazanego przez env_file:."
    },
    {
      "question": "Co oznacza składnia '${DB_password:?Brak hasla!}' w pliku Compose?",
      "options": {
        "A": "Podstawia napis 'Brak hasla!' jako wartość zmienną, jeśli DB_password nie istnieje",
        "B": "Ignoruje brak zmiennej i kontynuuje z pustą wartością",
        "C": "Zatrzymuje Compose z błędem zawierającym komunikat 'Brak hasla!', jeśli DB_password nie jest ustawiona",
        "D": "Automatycznie generuje losowe hasło, jeśli zmienna nie istnieje"
      },
      "correct": "C",
      "explanation": "Składnia :? oznacza zmienną wymaganą - jej brak powoduje zatrzymanie Compose z podanym komunikatem błędu, zamiast cichego kontynuowania z pustą wartością."
    },
    {
      "question": "Dlaczego commitowanie prawdziwego pliku '.env' z sekretami do repozytorium Git jest szczególnie niebezpieczne, nawet jeśli plik zostanie później usunięty?",
      "options": {
        "A": "Nie jest niebezpieczne - usunięcie pliku w kolejnym commicie całkowicie usuwa go z repozytorium",
        "B": "Historia commitów zachowuje starą wersję pliku na zawsze, więc sekret pozostaje dostępny mimo późniejszego usunięcia",
        "C": "Git automatycznie szyfruje wszystkie pliki .env, więc nie ma to znaczenia",
        "D": "Problem dotyczy tylko plików większych niż 100 MB"
      },
      "correct": "B",
      "explanation": "Historia Gita jest trwała - usunięcie pliku w nowym commicie nie usuwa jego poprzednich wersji z historii, więc sekret raz zacommitowany pozostaje odzyskiwalny."
    },
    {
      "question": "Jaki jest zalecany, dobry wzorzec zarządzania plikami z sekretami w projekcie korzystającym z Compose?",
      "options": {
        "A": "Trzymać prawdziwe wartości sekretów wprost w docker-compose.yml dla przejrzystości",
        "B": "Dodać .env i *.env do .gitignore, a commitować jedynie wzorcowy plik .env.example z samymi nazwami zmiennych",
        "C": "Wysyłać plik .env mailem do każdego nowego programisty w zespole",
        "D": "Nigdy nie używać zmiennych środowiskowych, tylko wartości wpisane wprost w kodzie aplikacji"
      },
      "correct": "B",
      "explanation": "Standardowy wzorzec to ignorowanie prawdziwych plików z sekretami w .gitignore i commitowanie jedynie przykładowego pliku dokumentującego, jakie zmienne trzeba uzupełnić lokalnie."
    },
    {
      "question": "Co wypisze kontener, jeśli w .env jest TAG=1.4.0?",
      "code": "services:\n  app:\n    image: alpine:3.20\n    command: [\"sh\",\"-c\",\"echo TAG=[$$TAG]\"]\n# .env: TAG=1.4.0",
      "options": {
        "A": "TAG=[] - .env służy do podstawień ${...} w pliku, nie jest automatycznie wstrzykiwany do kontenera",
        "B": "TAG=[1.4.0]",
        "C": "TAG=[$TAG]",
        "D": "Błąd interpolacji"
      },
      "correct": "A",
      "explanation": "$$ to dosłowny $ przekazany do powłoki kontenera. Zmienna nie istnieje w środowisku kontenera, dopóki nie przekażesz jej przez environment lub env_file."
    },
    {
      "question": "Jaką wartość PROFILE zobaczy kontener?",
      "code": "# wspolne.env: PROFILE=dev\nservices:\n  app:\n    env_file: [wspolne.env]\n    environment:\n      PROFILE: docker",
      "options": {
        "A": "dev",
        "B": "Pusta",
        "C": "dev,docker",
        "D": "docker - environment ma pierwszeństwo przed env_file"
      },
      "correct": "D",
      "explanation": "env_file dostarcza wartości bazowe, a environment je nadpisuje. Pozwala to trzymać wspólne ustawienia w pliku i zmieniać pojedyncze wartości per serwis."
    },
    {
      "question": "Jaka wartość POSTGRES_DB trafi do kontenera, jeśli DB_NAME nie jest nigdzie ustawione?",
      "code": "environment:\n  POSTGRES_DB: ${DB_NAME:-javaquest}",
      "options": {
        "A": "javaquest",
        "B": "${DB_NAME:-javaquest}",
        "C": "Pusta",
        "D": "Błąd"
      },
      "correct": "A",
      "explanation": ":- podstawia wartość domyślną, gdy zmienna jest nieustawiona lub pusta (sam '-' - tylko gdy nieustawiona)."
    },
    {
      "question": "Co zobaczysz, uruchamiając ten plik bez zmiennej DB_PASSWORD?",
      "code": "environment:\n  P: ${DB_PASSWORD:?Brak DB_PASSWORD}",
      "options": {
        "A": "Kontener wystartuje z pustym hasłem",
        "B": "Compose zapyta o hasło",
        "C": "Ostrzeżenie i hasło 'Brak DB_PASSWORD'",
        "D": "error while interpolating ...: required variable DB_PASSWORD is missing a value: Brak DB_PASSWORD"
      },
      "correct": "D",
      "explanation": "Błąd pojawia się już przy przetwarzaniu pliku (także w docker compose config), zanim cokolwiek zostanie uruchomione. To dużo lepsze niż baza z pustym hasłem."
    },
    {
      "question": "Skąd Compose weźmie wartość ${TAG}, jeśli jest ustawiona zarówno w powłoce, jak i w .env?",
      "code": "$ export TAG=2.0.0\n$ cat .env\nTAG=1.4.0\n$ docker compose config | grep image",
      "options": {
        "A": "2.0.0 - zmienne powłoki mają pierwszeństwo przed .env",
        "B": "1.4.0 - .env wygrywa",
        "C": "Obie",
        "D": "Błąd konfliktu"
      },
      "correct": "A",
      "explanation": "Plik .env dostarcza wartości domyślnych dla podstawień. Zmienne ustawione w powłoce (np. w CI) je nadpisują - wygodne do jednorazowej zmiany tagu."
    },
    {
      "question": "Jak użyć innego pliku zamiast .env do podstawień w compose.yaml?",
      "code": "$ docker compose ??? .env.test up -d",
      "options": {
        "A": "-e",
        "B": "--vars",
        "C": "--env-file",
        "D": "-f"
      },
      "correct": "C",
      "explanation": "--env-file wskazuje plik zmiennych do interpolacji. -f wskazuje plik Compose. Tak można trzymać .env.dev, .env.test itd. z tymi samymi kluczami."
    },
    {
      "question": "Który wpis .gitignore chroni sekrety, a jednocześnie pozwala commitować szablon?",
      "code": "# .gitignore\n???",
      "options": {
        "A": ".env.example",
        "B": "!.env",
        "C": "*.yaml",
        "D": ".env\\n*.env\\n!.env.example"
      },
      "correct": "D",
      "explanation": "Wyjątek '!.env.example' przywraca szablon do śledzenia. Szablon dokumentuje wymagane zmienne bez prawdziwych wartości."
    },
    {
      "question": "Gdzie w kontenerze pojawi się sekret zadeklarowany tak?",
      "code": "services:\n  baza:\n    secrets: [db_password]\nsecrets:\n  db_password:\n    file: ./secrets/db_password.txt",
      "options": {
        "A": "Jako plik /run/secrets/db_password",
        "B": "W zmiennej środowiskowej DB_PASSWORD",
        "C": "W /etc/passwd",
        "D": "W logach"
      },
      "correct": "A",
      "explanation": "Sekrety są montowane jako pliki tylko w kontenerach, które je deklarują - nie widać ich w docker inspect ani w env. Wiele obrazów obsługuje zmienne *_FILE (np. POSTGRES_PASSWORD_FILE)."
    },
    {
      "question": "Która zmienna pozwala obrazowi postgres odczytać hasło z pliku sekretu?",
      "code": "environment:\n  ???: /run/secrets/db_password",
      "options": {
        "A": "POSTGRES_PASSWORD",
        "B": "PGPASSFILE_SECRET",
        "C": "POSTGRES_PASSWORD_FILE",
        "D": "SECRET_POSTGRES"
      },
      "correct": "C",
      "explanation": "Konwencja *_FILE jest wspierana przez wiele oficjalnych obrazów (postgres, mysql, mariadb). Skrypt startowy czyta zawartość pliku zamiast wartości zmiennej."
    },
    {
      "question": "Dlaczego sekret w pliku jest bezpieczniejszy niż zmienna środowiskowa?",
      "code": "$ docker inspect --format '{{json .Config.Env}}' sklep-baza-1",
      "options": {
        "A": "Bo jest szyfrowany przez Compose",
        "B": "Nie ma różnicy",
        "C": "Bo pliki są szybsze",
        "D": "Bo zmienne środowiskowe są widoczne w docker inspect, w środowisku procesów i często trafiają do logów/zrzutów; plik jest dostępny tylko w kontenerach, które go deklarują"
      },
      "correct": "D",
      "explanation": "W Compose (poza Swarmem) sekret z file: to w praktyce bind mount, więc nie jest szyfrowany - ale ogranicza liczbę miejsc, w których wartość może wyciec."
    },
    {
      "question": "Co trafi do kontenera jako wartość HASLO?",
      "code": "environment:\n  HASLO: Pa$$word",
      "options": {
        "A": "Pa$word - w pliku Compose $$ oznacza dosłowny znak $",
        "B": "Pa$$word",
        "C": "Pa",
        "D": "Błąd"
      },
      "correct": "A",
      "explanation": "Compose interpoluje $ w pliku. Hasło zawierające $ trzeba w compose.yaml zapisać z podwojonymi znakami, a najlepiej trzymać je poza plikiem (env_file, sekret)."
    },
    {
      "question": "Który mechanizm wstrzykuje zmienne z pliku DO kontenera?",
      "code": "# (1) plik .env obok compose.yaml\n# (2) env_file: [api.env] w definicji serwisu",
      "options": {
        "A": "(1)",
        "B": "Oba",
        "C": "(2)",
        "D": "Żaden"
      },
      "correct": "C",
      "explanation": "Mylące podobieństwo nazw jest częstym źródłem błędów: .env zasila podstawienia w compose.yaml, env_file - środowisko kontenera."
    },
    {
      "question": "Jak Spring Boot może odczytać sekrety zamontowane jako pliki w /run/secrets?",
      "code": "environment:\n  SPRING_CONFIG_IMPORT: \"???\"",
      "options": {
        "A": "classpath:/run/secrets",
        "B": "secrets://run",
        "C": "file:/run/secrets.properties",
        "D": "configtree:/run/secrets/"
      },
      "correct": "D",
      "explanation": "configtree traktuje każdy plik w katalogu jako właściwość (nazwa pliku = klucz, zawartość = wartość). Ten sam mechanizm działa z sekretami Kubernetesa."
    },
    {
      "question": "Czy docker compose config może ujawnić sekrety?",
      "code": "$ docker compose config > konfiguracja.txt\n$ git add konfiguracja.txt",
      "options": {
        "A": "Tak - wypisuje konfigurację z już podstawionymi wartościami, także z .env; wyniku nie wolno commitować ani wklejać publicznie",
        "B": "Nie - config maskuje hasła",
        "C": "Tylko przy --verbose",
        "D": "Tylko na Linuksie"
      },
      "correct": "A",
      "explanation": "config jest świetne do debugowania, ale jego wynik należy traktować jak plik z sekretami."
    },
    {
      "question": "Hasło z .env zostało wypchnięte na GitHub. Co zrobić NAJPIERW?",
      "code": "$ git log --oneline -- .env\na1b2c3d dodaj konfiguracje",
      "options": {
        "A": "git rm .env i push",
        "B": "Usunąć repozytorium",
        "C": "Zmienić (unieważnić) hasło w systemie, do którego należy - dopiero potem sprzątać repozytorium",
        "D": "Przepisać historię przez rebase"
      },
      "correct": "C",
      "explanation": "Historia Gita i kopie u innych osób nadal zawierają hasło. Jedynym pewnym zabezpieczeniem jest sprawienie, że przestaje ono działać."
    },
    {
      "question": "Jaki jest efekt wpisania pustej wartości w .env przy składni ${VAR:-domyslna}?",
      "code": "# .env\nDB_NAME=\n# compose.yaml\nPOSTGRES_DB: ${DB_NAME:-javaquest}",
      "options": {
        "A": "Pusta nazwa bazy",
        "B": "DB_NAME",
        "C": "Błąd",
        "D": "javaquest - ':-' traktuje pustą wartość jak brak"
      },
      "correct": "D",
      "explanation": "Wariant bez dwukropka (${DB_NAME-javaquest}) użyłby wartości domyślnej tylko przy braku zmiennej, a pustą wartość przekazałby dalej."
    },
    {
      "question": "Czy zmienne z env_file są dostępne do podstawień ${...} w samym pliku compose.yaml?",
      "code": "services:\n  app:\n    env_file: [api.env]        # api.env: PORT=9090\n    ports: [\"${PORT}:9090\"]",
      "options": {
        "A": "Nie - env_file zasila tylko środowisko kontenera; podstawienia korzystają z powłoki i pliku .env (lub --env-file)",
        "B": "Tak",
        "C": "Tylko dla portów",
        "D": "Tylko w Compose v1"
      },
      "correct": "A",
      "explanation": "Dwa mechanizmy działają na różnych etapach: interpolacja pliku (przed utworzeniem kontenerów) i środowisko procesu w kontenerze (po utworzeniu)."
    },
    {
      "question": "Który sposób przekazania JWT_SECRET jest najmniej ryzykowny z wymienionych?",
      "code": "# (1) JWT_SECRET: \"a8f5f167...\" wprost w compose.yaml (commitowanym)\n# (2) JWT_SECRET: ${JWT_SECRET:?} z .env poza repozytorium\n# (3) sekret Compose montowany jako plik + configtree",
      "options": {
        "A": "(1)",
        "B": "(2)",
        "C": "(3)",
        "D": "Wszystkie jednakowo"
      },
      "correct": "C",
      "explanation": "(1) trafia do historii Gita. (2) jest poza repozytorium, ale wartość jest widoczna w środowisku kontenera. (3) ogranicza widoczność do pliku w wybranych kontenerach - ten sam wzorzec stosuje się potem w Kubernetesie."
    },
    {
      "question": "Jak nadpisać jedną zmienną tylko dla jednorazowego uruchomienia polecenia w serwisie?",
      "code": "$ docker compose run --rm ??? SPRING_PROFILES_ACTIVE=test api",
      "options": {
        "A": "--env",
        "B": "-e",
        "C": "--set",
        "D": "--var"
      },
      "correct": "B",
      "explanation": "run -e ma najwyższy priorytet - nadpisuje zarówno environment, jak i env_file dla tego jednego kontenera. Plik Compose się nie zmienia."
    },
    {
      "question": "Które pliki powinny trafić do .dockerignore w projekcie z Compose?",
      "code": "projekt/\n├── .env\n├── .env.example\n├── secrets/db_password.txt\n└── src/",
      "options": {
        "A": "Żadne - .dockerignore dotyczy tylko obrazów bazowych",
        "B": ".env i katalog secrets/ - inaczej przy COPY . . mogłyby trafić do obrazu",
        "C": "Tylko src/",
        "D": "Tylko .env.example"
      },
      "correct": "B",
      "explanation": ".gitignore nie wpływa na kontekst budowania. Sekrety w obrazie są dostępne dla każdego, kto ma obraz - także w rejestrze."
    }
  ]
}
