{
  "theory": [
    {
      "type": "INTRO",
      "heading": "Nie wszystko w folderze projektu powinno trafić do Gita",
      "body": "Każdy projekt gromadzi pliki, których Git NIGDY nie powinien śledzić: skompilowane `.class`, folder `target/` z buildu Mavena, ustawienia IDE (`.idea/`), pliki z hasłami/kluczami API (`.env`). `.gitignore` to lista wzorców mówiąca Gitowi 'nie pokazuj mi tych plików jako nieśledzone, nie pozwól ich przypadkiem dodać'. `.gitattributes` to POKREWNY, ale INNY plik — nie 'ukrywa' plików, tylko przypisuje im SPECJALNE ZACHOWANIE (np. jak traktować końce linii, czy plik jest binarny). Oba pliki same w sobie SĄ commitowane (jako część projektu) i współdzielone z całym zespołem.",
      "code": null
    },
    {
      "type": "DEFINITION",
      "heading": "`.gitignore` — lista wzorców do pominięcia; `.gitattributes` — instrukcje obsługi",
      "body": "`.gitignore` to zwykły plik tekstowy (w katalogu głównym repozytorium, albo w dowolnym podfolderze — obowiązuje wtedy TYLKO dla tego podfolderu) z LISTĄ wzorców ścieżek — pliki/foldery pasujące do wzorca Git POMIJA CAŁKOWICIE: nie pokazuje ich jako 'untracked' w `git status` (lekcja 1), odmawia dodania nawet przez jawne `git add` (chyba że użyjesz `-f`/`--force`). `.gitattributes` przypisuje ATRYBUTY konkretnym ścieżkom (też przez wzorce) — np. 'ten typ pliku ZAWSZE ma końce linii LF, niezależnie od systemu operacyjnego', 'ten plik jest BINARNY, nie próbuj pokazywać go jako tekstowy diff', 'ten wygenerowany plik nie powinien liczyć się do statystyk języków repozytorium'.",
      "code": null
    },
    {
      "type": "ANALOGY",
      "heading": "Lista 'wstęp wzbroniony' kontra etykieta 'ostrożnie, szkło'",
      "body": "`.gitignore` to jak lista na drzwiach klubu: rzeczy na tej liście NIGDY nie wchodzą do środka (Git nawet nie rozważa ich dodania) — nie musisz ich pilnować, po prostu ICH TAM NIE MA. `.gitattributes` to zupełnie inna sprawa: dotyczy RZECZY, KTÓRE JUŻ SĄ W ŚRODKU (śledzonych plików), ale mają DOŁĄCZONĄ etykietę z instrukcją obsługi — 'to jest szkło, obchodź się ostrożnie' (ten plik jest binarny, nie próbuj go 'scalać' tekstowo), albo 'to opakowanie zbiorcze, nie liczy się do inwentarza pojedynczych sztuk' (plik wygenerowany, pomiń w statystykach).",
      "code": null
    },
    {
      "type": "VISUAL_EXAMPLE",
      "heading": "Typowy `.gitignore` i `.gitattributes` projektu Java/Maven",
      "body": ".gitignore:\ntarget/                 # cala zawartosc folderu buildu Mavena\n*.class                 # skompilowane klasy Javy, gdziekolwiek by nie byly\n.idea/                  # ustawienia projektu IntelliJ IDEA\n*.iml                   # pliki modulu IntelliJ\n.env                    # zmienne srodowiskowe / sekrety - NIGDY w repo\n!.env.example           # WYJATEK (!): TEN plik (szablon BEZ prawdziwych sekretow) - sledz normalnie\n\n.gitattributes:\n*.sh text eol=lf        # skrypty shell ZAWSZE z koncami linii LF (nawet edytowane na Windows)\n*.bat text eol=crlf     # skrypty .bat ZAWSZE z koncami linii CRLF\n*.jar binary            # archiwa JAR - traktuj jako binarne, nie tekstowe (bez prob 'diff')\n*.png binary\ndocs/wygenerowane/** linguist-generated=true   # nie licz do statystyk jezykow (np. na GitHub)",
      "code": null
    },
    {
      "type": "CODE_BASIC",
      "heading": "Podstawowa składnia wzorców `.gitignore`",
      "body": "Kilka najczęściej używanych form wzorców — od prostych nazw plików po pełne ścieżki i wyjątki.",
      "code": "*.log                  # WSZYSTKIE pliki z rozszerzeniem .log, w KAZDYM folderze\n/build                 # TYLKO folder 'build' w KATALOGU GLOWNYM (poczatkowy '/' = brak rekurencji w gore)\ntemp/                  # folder 'temp' GDZIEKOLWIEK w projekcie (koncowy '/' = TYLKO folder, nie plik)\n!wazny.log              # WYJATEK: ten KONKRETNY plik SLEDZ, mimo ze pasuje do wzorca '*.log' wyzej\n# To jest komentarz - linie zaczynajace sie od '#' sa ignorowane przez sam .gitignore"
    },
    {
      "type": "CODE_PRACTICAL",
      "heading": "Debugowanie — dlaczego dany plik JEST (albo NIE JEST) ignorowany",
      "body": "Przy wielu regułach w `.gitignore` (zwłaszcza w większym projekcie z wieloma modułami) czasem trudno zgadnąć, KTÓRA reguła odpowiada za zignorowanie (albo nieoczekiwane nieignorowanie) konkretnego pliku — `git check-ignore` odpowiada wprost.",
      "code": "git check-ignore -v target/classes/Main.class\n# .gitignore:2:target/    target/classes/Main.class\n# (pokazuje: PLIK .gitignore, NUMER LINII, TRESC REGULY, ktora spowodowala zignorowanie)\n\ngit status --ignored                 # pokaz TAKZE zignorowane pliki (normalnie ukryte)"
    },
    {
      "type": "STEP_BY_STEP",
      "heading": "Dlaczego dodanie wzorca do `.gitignore` NIE 'odśledza' już śledzonego pliku",
      "body": "1. Plik `Sekrety.properties` został PRZYPADKOWO zacommitowany DAWNO TEMU (zanim ktokolwiek pomyślał o `.gitignore`) — Git go już ŚLEDZI, jest częścią historii.\n2. Ktoś DZISIAJ dodaje `Sekrety.properties` do `.gitignore`, myśląc 'teraz Git go zignoruje'.\n3. `.gitignore` wpływa WYŁĄCZNIE na pliki, które są JESZCZE NIEŚLEDZONE (untracked) — dla pliku, który JUŻ jest częścią repozytorium, wpis w `.gitignore` NIE MA ŻADNEGO EFEKTU: Git nadal go śledzi, nadal pokazuje jego zmiany, nadal go commituje przy zwykłym `git add .`/`git commit -a`.\n4. Żeby FAKTYCZNIE przestać śledzić już śledzony plik (zachowując go NA DYSKU), trzeba jawnie: `git rm --cached Sekrety.properties` — to USUWA plik ZE śledzenia (z indeksu), ale NIE kasuje go z working directory.\n5. DOPIERO TERAZ wpis w `.gitignore` zacznie działać — kolejne zmiany w tym pliku będą ignorowane. WAŻNE: plik WCIĄŻ istnieje w CAŁEJ WCZEŚNIEJSZEJ historii commitów (lekcja 2) — jeśli zawierał sekrety, one WCIĄŻ tam są, dostępne każdemu, kto przejrzy historię (rozwinięcie w bloku PITFALL).",
      "code": null
    },
    {
      "type": "NOTE",
      "heading": "`.gitignore` działa TYLKO na pliki nieśledzone — to jedyna, kluczowa zasada tej lekcji",
      "body": "Cała lekcja sprowadza się do JEDNEGO, fundamentalnego rozróżnienia: `.gitignore` to filtr na WEJŚCIU (zapobiega temu, żeby coś w ogóle zaczęło być śledzone), NIE mechanizm 'wymazywania' czegoś, co już jest śledzone. Dokładnie tak samo jak lista 'wstęp wzbroniony' na drzwiach klubu nie wyrzuca nikogo, kto już jest W ŚRODKU — działa TYLKO na nowe osoby próbujące wejść.",
      "code": null
    },
    {
      "type": "USAGE",
      "heading": "Gdzie te pliki realnie się stosują",
      "body": "`.gitignore` — utrzymanie repozytorium CZYSTEGO z artefaktów budowania (`target/`, `*.class`, `node_modules/`), ustawień prywatnego IDE, i (KRYTYCZNIE) plików z sekretami/konfiguracją lokalną (`.env`, `application-local.properties`). Serwis `gitignore.io`/szablony GitHuba dostarczają GOTOWE wzorce dla popularnych stosów technologicznych (Java, Maven, IntelliJ). `.gitattributes` — spójne końce linii w zespole mieszającym Windows/Linux/Mac (zamiast polegać wyłącznie na lokalnej konfiguracji `core.autocrlf` każdego programisty, `.gitattributes` WYMUSZA to SAMO zachowanie dla WSZYSTKICH, bo plik jest częścią repozytorium), oznaczanie plików wygenerowanych/vendorowanych, żeby nie zaśmiecały statystyk języków repozytorium na GitHub.",
      "code": null
    },
    {
      "type": "PITFALL",
      "heading": "Zacommitowany sekret pozostaje w historii NA ZAWSZE, nawet po usunięciu pliku",
      "body": "Jeśli plik z PRAWDZIWYM hasłem/kluczem API zostanie PRZYPADKOWO zacommitowany (przed dodaniem go do `.gitignore`), samo dodanie wzorca do `.gitignore` i usunięcie pliku (`git rm`) NIE USUWA sekretu z HISTORII — commit, w którym plik ZOSTAŁ dodany, WCIĄŻ istnieje i WCIĄŻ zawiera oryginalną treść (dokładnie ta sama zasada z lekcji 2: commity są migawkami, nie da się ich 'wyczyścić' bez przepisania historii). KAŻDY, kto sklonuje repozytorium (albo miał je już sklonowane) MOŻE cofnąć się do tamtego commita (`git checkout <hash>`, lekcja 2) i zobaczyć oryginalny sekret — nawet jeśli AKTUALNY stan `main` go już nie zawiera.",
      "code": null
    },
    {
      "type": "CODE_WRONG",
      "heading": "'Naprawienie' wycieku sekretu przez samo usunięcie pliku",
      "body": "To wygląda na naprawienie problemu ('plik zniknął, `.gitignore` go teraz pilnuje'), ale HASŁO WCIĄŻ ISTNIEJE w historii, w commicie `a1b2c3d` — każdy z dostępem do repozytorium (albo jego klonu sprzed 'naprawy') może je odnaleźć.",
      "code": "# a1b2c3d: PRZYPADKOWO zacommitowano .env z prawdziwym haslem do bazy danych\ngit rm .env\necho \".env\" >> .gitignore\ngit add .gitignore\ngit commit -m \"Usun .env z repo, dodaj do gitignore\"\ngit push\n# BLEDNE zalozenie: 'sekret jest bezpieczny, bo plik juz nie istnieje na main'\n# Haslo WCIAZ jest widoczne w historii: 'git show a1b2c3d:.env' zwroci oryginalna tresc"
    },
    {
      "type": "CODE_RIGHT",
      "heading": "Prawdziwa naprawa — rotacja sekretu JEST najważniejszym krokiem",
      "body": "Usunięcie pliku z bieżącego stanu i dodanie do `.gitignore` to poprawne KROKI, ale NIEWYSTARCZAJĄCE — NAJWAŻNIEJSZYM działaniem jest UNIEWAŻNIENIE (rotacja) samego wyciekłego sekretu, żeby stara wartość, WIDOCZNA W HISTORII, przestała mieć jakąkolwiek wartość dla kogokolwiek, kto ją znajdzie.",
      "code": "git rm --cached .env                 # przestan sledzic, ZACHOWAJ plik lokalnie na dysku\necho \".env\" >> .gitignore\ngit add .gitignore\ngit commit -m \"Przestan sledzic .env, dodaj do gitignore\"\n\n# NAJWAZNIEJSZY krok, NIEZALEZNY od samego Gita:\n# NATYCHMIAST zmien/uniewaznij haslo do bazy danych w systemie zarzadzania sekretami\n# (stara wartosc w historii Gita staje sie WTEDY bezuzyteczna dla kogokolwiek, kto ja znajdzie)\n#\n# (Pelne usuniecie sekretu Z SAMEJ historii wymaga przepisania jej narzedziami jak\n# 'git filter-repo'/BFG Repo-Cleaner - zlozona, ryzykowna operacja, poza zakresem tej lekcji -\n# rotacja sekretu jest SZYBSZA i WYSTARCZAJACA w zdecydowanej wiekszosci przypadkow)"
    },
    {
      "type": "API_REFERENCE",
      "heading": "Podstawowe narzędzia i składnia",
      "body": "Wzorce `.gitignore`: `*` (dowolny ciąg znaków w NAZWIE, nie przechodzi przez `/`), `**` (dowolna głębokość folderów), początkowe `/` (kotwica do katalogu, w którym leży `.gitignore`), końcowe `/` (dopasowuje TYLKO foldery), `!` (wyjątek — jednak śledź, mimo pasowania do wcześniejszej reguły). `git check-ignore -v <plik>` (pokaż, KTÓRA reguła i z którego pliku ignoruje dany plik). `git status --ignored` (pokaż też zignorowane pliki). `git rm --cached <plik>` (przestań śledzić, zachowaj na dysku). Globalny `.gitignore` (`git config --global core.excludesfile ~/.gitignore_global`) — dla PRYWATNYCH wzorców (ustawienia Twojego IDE) NIEZALEŻNYCH od konkretnego projektu, nie commitowanych do repo.",
      "code": null
    },
    {
      "type": "WHEN_TO_USE",
      "heading": "Projektowy `.gitignore` kontra globalny kontra `.git/info/exclude`",
      "body": "Projektowy `.gitignore` (w repo, WSPÓŁDZIELONY) — dla wzorców ISTOTNYCH dla całego zespołu, niezależnie od czyjegoś IDE (`target/`, `*.class`, `.env`) — WSZYSCY korzystają z TYCH SAMYCH reguł. Globalny `.gitignore` (`~/.gitignore_global`, poza repozytorium) — dla TWOICH prywatnych preferencji IDE/OS (`.DS_Store` na macOS, pliki `.idea/` jeśli reszta zespołu używa Eclipse) — nie zaśmieca projektowego `.gitignore` czymś, co dotyczy TYLKO Ciebie. `.git/info/exclude` (lokalny, per-repozytorium, NIGDY commitowany) — dla jednorazowych, PRYWATNYCH wzorców dotyczących TYLKO tego jednego projektu na TWOIM dysku, których nie chcesz narzucać reszcie zespołu.",
      "code": null
    },
    {
      "type": "SUMMARY",
      "heading": "Podsumowanie",
      "body": "`.gitignore` to lista wzorców plików, których Git ma NIGDY nie zaczynać śledzić — działa WYŁĄCZNIE na pliki JESZCZE nieśledzone; dla pliku już śledzonego trzeba jawnie `git rm --cached`. `.gitattributes` przypisuje ścieżkom SPECJALNE zachowanie (końce linii, status binarny, wykluczenie ze statystyk) dla plików, które Git JUŻ śledzi. Kluczowa pułapka: zacommitowany sekret POZOSTAJE w historii na zawsze, nawet po usunięciu pliku i dodaniu go do `.gitignore` — jedyną PEWNĄ naprawą jest natychmiastowa ROTACJA (unieważnienie) wyciekłego sekretu, nie samo 'sprzątnięcie' repozytorium. Trzy poziomy ignorowania (projektowy/globalny/lokalny) rozdzielają reguły WSPÓLNE dla zespołu od PRYWATNYCH preferencji programisty.",
      "code": null
    }
  ],
  "exercises": [
    {
      "prompt": "Wyjaśnij (jako String), dlaczego dodanie wzorca `*.log` do `.gitignore` nie powoduje, że plik `aplikacja.log`, który był zacommitowany dwa tygodnie temu, przestaje być śledzony przez Git.",
      "hint": "Przypomnij sobie z bloku note, na jakim etapie 'cyklu zycia' pliku (przed czy po pierwszym dodaniu/commicie) '.gitignore' w ogole ma jakikolwiek wplyw.",
      "solution": "`.gitignore` działa WYŁĄCZNIE jako filtr na WEJŚCIU - wpływa tylko na pliki, które są JESZCZE nieśledzone (untracked), zapobiegając ich przypadkowemu dodaniu. Plik `aplikacja.log`, zacommitowany dwa tygodnie temu, jest JUŻ częścią historii repozytorium (śledzony) - dodanie wzorca `*.log` do `.gitignore` DZISIAJ nie ma na niego żadnego wpływu, bo reguła ta w ogóle nie jest sprawdzana dla plików już będących pod kontrolą wersji. Żeby faktycznie przestać śledzić ten konkretny plik, trzeba jawnie wykonać `git rm --cached aplikacja.log`."
    },
    {
      "prompt": "Napisz (jako String) sekwencję poleceń potrzebną do prawidłowego naprawienia sytuacji, w której plik `sekrety.properties` z prawdziwym kluczem API został przypadkowo zacommitowany tydzień temu - uwzględnij wszystkie niezbędne kroki, nie tylko te dotyczące samego Gita.",
      "hint": "Skorzystaj z sekwencji z bloku code_right - pamietaj, ze sam Git nie wystarczy, bo historia pozostanie 'skazona' oryginalnym sekretem.",
      "solution": "Pełna sekwencja: (1) `git rm --cached sekrety.properties` (przestań śledzić, zachowaj plik lokalnie na dysku), (2) dodaj `sekrety.properties` do `.gitignore`, (3) `git add .gitignore && git commit -m \"Przestan sledzic sekrety.properties\"` i `git push` - to CZĘŚĆ Gitowa. (4) NAJWAŻNIEJSZY krok, NIEZALEŻNY od Gita: NATYCHMIAST zmień/unieważnij dany klucz API w systemie, który go wydał - stara wartość, wciąż widoczna w historii commitów (dostępna każdemu z dostępem do repozytorium, nawet po usunięciu pliku z bieżącego stanu), staje się wtedy bezużyteczna dla kogokolwiek, kto by ją znalazł. Pominięcie kroku (4) zostawia realne ryzyko bezpieczeństwa, mimo że repozytorium 'wygląda' na naprawione."
    },
    {
      "prompt": "Wyjaśnij (jako String) różnicę między projektowym `.gitignore` (w repozytorium, commitowanym) a globalnym `.gitignore` (`~/.gitignore_global`) - kiedy dana reguła powinna trafić do jednego, a kiedy do drugiego?",
      "hint": "Zastanow sie, czy dana regula (np. 'ignoruj .idea/') dotyczy wszystkich osob pracujacych nad tym projektem, czy jest to twoja wlasna, prywatna preferencja narzedziowa, ktora niekoniecznie dotyczy innych czlonkow zespolu (np. uzywajacych innego IDE).",
      "solution": "Reguła powinna trafić do projektowego `.gitignore` (commitowanego, współdzielonego), jeśli dotyczy CZEGOŚ ISTOTNEGO dla całego zespołu, niezależnie od preferencji poszczególnych osób - np. `target/` (folder buildu Mavena, identyczny dla każdego, kto buduje ten projekt) czy `.env` (sekrety, które NIGDY nie powinny trafić do repo, niezależnie kto commituje). Reguła powinna trafić do GLOBALNEGO `.gitignore` (poza repozytorium, dotyczącego wszystkich Twoich projektów), jeśli jest to TWOJA prywatna preferencja narzędziowa - np. `.DS_Store` (artefakt macOS, nieistotny dla współpracowników na Windows) albo pliki konkretnego IDE, którego Ty używasz, ale reszta zespołu niekoniecznie - umieszczenie TAKICH wzorców w projektowym `.gitignore` niepotrzebnie 'zaśmiecałoby' go czymś, co dotyczy tylko Ciebie."
    },
    {
      "prompt": "Wyjaśnij (jako String), do czego służy `.gitattributes` z regułą `*.jar binary`, i co konkretnie by się stało (czego by brakowało), gdyby tej reguły nie było, a Git próbował potraktować plik JAR jak zwykły plik tekstowy przy operacjach takich jak `git diff` czy merge.",
      "hint": "Pomysl, czym faktycznie jest plik .jar (skompresowane archiwum binarne, nie tekst) i co by sie stalo, gdyby Git probowal wyswietlic 'linijkowy' diff takiego pliku, albo (gorzej) probowal go 'scalic' tekstowo przy konflikcie merge'a.",
      "solution": "Reguła `*.jar binary` mówi Gitowi WPROST, że plik JAR (skompresowane archiwum binarne, nie tekst) ma być traktowany jako binarny, a nie tekstowy. Bez tej reguły, Git mógłby PRÓBOWAĆ potraktować go jak tekst przy operacjach takich jak `git diff` (dając bezużyteczny, nieczytelny 'diff' losowych bajtów binarnych zinterpretowanych jako znaki) albo, co GORSZE, przy próbie AUTOMATYCZNEGO scalenia (merge) dwóch wersji tego samego pliku JAR z różnych gałęzi - tekstowe scalanie linia po linii binarnego archiwum niemal na pewno wyprodukowałoby USZKODZONY, bezużyteczny plik JAR, zamiast poprawnie rozpoznać sytuację jako konflikt wymagający wyboru JEDNEJ z dwóch całych wersji pliku."
    }
  ],
  "quiz": [
    {
      "question": "Na jakim etapie 'życia' pliku `.gitignore` ma wpływ na jego śledzenie przez Git?",
      "options": {
        "A": "Zawsze, niezależnie od tego, czy plik jest już śledzony, czy nie",
        "B": "Wyłącznie zanim plik zostanie po raz pierwszy dodany/zacommitowany - nie działa na pliki już śledzone",
        "C": "Wyłącznie po tym, jak plik zostanie zacommitowany",
        "D": "Tylko na pliki większe niż 1 MB"
      },
      "correct": "B",
      "explanation": ".gitignore działa jako filtr na wejściu - wpływa wyłącznie na pliki jeszcze nieśledzone. Dla pliku już będącego częścią repozytorium nie ma żadnego efektu, dopóki nie zostanie jawnie usunięty ze śledzenia przez git rm --cached."
    },
    {
      "question": "Co należy zrobić, żeby przestać śledzić plik, który jest już częścią historii repozytorium, zachowując go jednocześnie na dysku?",
      "options": {
        "A": "Wystarczy dodać go do .gitignore",
        "B": "git rm --cached <plik>, a następnie dodać go do .gitignore",
        "C": "Usunąć cały folder .git i zainicjować repozytorium od nowa",
        "D": "Zmienić rozszerzenie pliku"
      },
      "correct": "B",
      "explanation": "Samo dodanie do .gitignore nie ma efektu na już śledzone pliki - trzeba jawnie usunąć je ze śledzenia przez git rm --cached (co zachowuje plik na dysku), a dopiero potem wpis w .gitignore zacznie działać dla przyszłych zmian."
    },
    {
      "question": "Dlaczego samo usunięcie pliku z sekretem i dodanie go do .gitignore nie jest wystarczającą naprawą wycieku danych wrażliwych?",
      "options": {
        "A": "Jest to całkowicie wystarczające i bezpieczne rozwiązanie",
        "B": "Commit, w którym sekret został pierwotnie dodany, wciąż istnieje w historii i zawiera oryginalną treść - każdy z dostępem do repozytorium może ją odnaleźć",
        "C": "Git automatycznie usuwa stare commity po pewnym czasie",
        "D": "To niemożliwe - Git nie pozwala na commitowanie plików z sekretami"
      },
      "correct": "B",
      "explanation": "Commity są niezmiennymi migawkami - usunięcie pliku z bieżącego stanu nie usuwa go z historii, więc oryginalny sekret pozostaje dostępny w starszych commitach, dostępnych każdemu z dostępem do repozytorium."
    },
    {
      "question": "Jaki jest najważniejszy krok po przypadkowym zacommitowaniu prawdziwego hasła/klucza API?",
      "options": {
        "A": "Wyłącznie usunięcie pliku z repozytorium",
        "B": "Natychmiastowa rotacja (unieważnienie) wyciekłego sekretu, żeby stara wartość widoczna w historii przestała mieć jakąkolwiek wartość",
        "C": "Zmiana nazwy repozytorium",
        "D": "Poinformowanie wyłącznie bezpośredniego przełożonego, bez żadnych działań technicznych"
      },
      "correct": "B",
      "explanation": "Ponieważ sekret pozostaje w historii niezależnie od usunięcia pliku, jedynym pewnym zabezpieczeniem jest unieważnienie/rotacja samego sekretu - wtedy stara wartość w historii staje się bezużyteczna."
    },
    {
      "question": "Do czego służy plik `.gitattributes`?",
      "options": {
        "A": "Do ukrywania plików przed Gitem, tak jak .gitignore",
        "B": "Do przypisywania specjalnego zachowania (np. końce linii, status binarny) plikom, które Git już śledzi",
        "C": "Do przechowywania haseł dostępu do repozytorium",
        "D": "Do automatycznego usuwania starych commitów"
      },
      "correct": "B",
      "explanation": ".gitattributes dotyczy plików już śledzonych przez Git, przypisując im dodatkowe instrukcje obsługi (normalizacja końców linii, oznaczenie jako plik binarny, wykluczenie ze statystyk językowych), w przeciwieństwie do .gitignore, który zapobiega śledzeniu."
    },
    {
      "question": "Kiedy warto umieścić regułę w globalnym .gitignore zamiast w projektowym?",
      "options": {
        "A": "Zawsze - globalny .gitignore powinien zawierać wszystkie reguły",
        "B": "Gdy reguła dotyczy prywatnej preferencji narzędziowej danego programisty (np. artefakty konkretnego IDE lub systemu operacyjnego), a nie czegoś istotnego dla całego zespołu",
        "C": "Nigdy - globalny .gitignore jest przestarzałym mechanizmem",
        "D": "Wyłącznie gdy repozytorium nie ma skonfigurowanego remote'a"
      },
      "correct": "B",
      "explanation": "Globalny .gitignore służy do prywatnych preferencji (np. pliki specyficzne dla Twojego IDE czy systemu operacyjnego), które nie muszą być narzucane całemu zespołowi - reguły istotne dla wszystkich powinny trafiać do współdzielonego, projektowego .gitignore."
    }
  ]
}
