{
  "theory": [
    {
      "type": "INTRO",
      "heading": "Kapston: JavaQuest Tasks API od zera do w pełni skonteneryzowanego systemu",
      "body": "Ten rozdział nauczył 10 osobnych elementów: kontenery vs VM (1), obrazy i tagi (2), Dockerfile (3), uruchamianie kontenerów (4), warstwy i cache (5), zmienne środowiskowe (6), wolumeny (7), sieci (8), multi-stage build (9), bezpieczeństwo (10). Prawdziwe wdrożenie ŁĄCZY WSZYSTKIE naraz: aplikację JavaQuest Tasks API i jej bazę danych PostgreSQL, skonteneryzowane, połączone siecią, z trwałymi danymi, bezpieczną konfiguracją i bez narzędzi budowania w obrazie produkcyjnym. Ten kapston przechodzi przez CAŁY proces, od Dockerfile'a po działający, dwukontenerowy system.",
      "code": null
    },
    {
      "type": "DEFINITION",
      "heading": "Cel: dwa kontenery, jedna sieć, jeden wolumen, zero sekretów w obrazie",
      "body": "Docelowy system: kontener `baza` (PostgreSQL, z NAZWANYM wolumenem dla trwałości danych, lekcja 7), kontener `tasks-api` (zbudowany z MULTI-STAGE Dockerfile'a — JDK+Maven w etapie budowania, TYLKO JRE w finalnym obrazie, lekcja 9 — działający jako NIEUPRZYWILEJOWANY użytkownik, lekcja 10), OBA podłączone do WSPÓLNEJ, nazwanej sieci `javaquest-net` (lekcja 8), komunikujące się PO NAZWACH kontenerów, NIE przez `localhost`. Dane logowania do bazy PRZEKAZYWANE przy `docker run` (lekcja 6), NIGDY zapisane w obrazie.",
      "code": null
    },
    {
      "type": "ANALOGY",
      "heading": "Dzień przeprowadzki — wszystkie spakowane pudła trafiają do właściwych pokoi naraz",
      "body": "Każda z 10 lekcji tego rozdziału spakowała JEDNO pudło: 'jak działają kontenery', 'jak budować obrazy', 'jak je uruchamiać', 'jak nie tracić danych', 'jak kontenery się odnajdują'... Kapston to DZIEŃ PRZEPROWADZKI — wszystkie te pudła trafiają NARAZ do nowego domu (działającego systemu), każde do WŁAŚCIWEGO pokoju, we WŁAŚCIWEJ kolejności (najpierw meble, potem pudła z rzeczami — analogicznie: najpierw baza danych, POTEM aplikacja, rozwinięcie w bloku PITFALL). Przeprowadzka zrobiona `w pojedynkę, bez planu` (uruchomienie wszystkiego naraz, bez przemyślanej kolejności) kończy się chaosem — DOKŁADNIE tak, jak nieskoordynowane uruchomienie kontenerów.",
      "code": null
    },
    {
      "type": "VISUAL_EXAMPLE",
      "heading": "Pełny obraz systemu — sieć, wolumen, dwa kontenery",
      "body": "[Siec 'javaquest-net' (lekcja 8)]\n  |\n  +-- kontener 'baza' (postgres:16, lekcja 2 - PRZYPIETA wersja)\n  |     -> wolumen 'pgdata' (lekcja 7) - dane PRZETRWAJA usuniecie kontenera\n  |     -> BEZ '-p' - niedostepna z hosta, TYLKO dla 'tasks-api' na tej samej sieci\n  |\n  +-- kontener 'tasks-api' (zbudowany z multi-stage Dockerfile, lekcja 9)\n        -> dziala jako NIEUPRZYWILEJOWANY uzytkownik (lekcja 10)\n        -> '-p 8080:8080' (lekcja 4) - DOSTEPNA z hosta pod localhost:8080\n        -> polaczenie z baza przez 'jdbc:postgresql://baza:5432/...' (NAZWA kontenera, lekcja 8)\n        -> dane logowania przez '-e'/'--env-file' (lekcja 6), ZERO sekretow w obrazie",
      "code": null
    },
    {
      "type": "CODE_BASIC",
      "heading": "Finalny Dockerfile — synteza lekcji 2, 3, 9, 10",
      "body": "Przypięta wersja, multi-stage, nieuprzywilejowany użytkownik — WSZYSTKO naraz, w JEDNYM Dockerfile'u.",
      "code": "FROM eclipse-temurin:21.0.2_13-jdk AS build\nWORKDIR /app\nCOPY mvnw pom.xml ./\nCOPY .mvn .mvn\nRUN ./mvnw dependency:go-offline\nCOPY src ./src\nRUN ./mvnw package -DskipTests\n\nFROM eclipse-temurin:21.0.2_13-jre\nRUN groupadd -r aplikacja && useradd -r -g aplikacja aplikacja\nWORKDIR /app\nCOPY --from=build --chown=aplikacja:aplikacja /app/target/javaquest-tasks-api.jar app.jar\nUSER aplikacja\nEXPOSE 8080\nENTRYPOINT [\"java\", \"-jar\", \"app.jar\"]"
    },
    {
      "type": "CODE_PRACTICAL",
      "heading": "Pełna sekwencja uruchomienia — sieć, wolumen, oba kontenery",
      "body": "Kolejność MA znaczenie (rozwinięcie w bloku PITFALL) — sieć i baza danych PRZED aplikacją, NIGDY odwrotnie.",
      "code": "docker network create javaquest-net\n\ndocker volume create pgdata\ndocker run -d --name baza --network javaquest-net -v pgdata:/var/lib/postgresql/data \\\n  -e POSTGRES_PASSWORD=haslo -e POSTGRES_DB=javaquest postgres:16\n\ndocker build -t javaquest-tasks-api:1.0 .\n\ndocker run -d --name tasks-api --network javaquest-net -p 8080:8080 \\\n  -e SPRING_DATASOURCE_URL=jdbc:postgresql://baza:5432/javaquest \\\n  -e SPRING_DATASOURCE_PASSWORD=haslo \\\n  javaquest-tasks-api:1.0\n\ncurl http://localhost:8080/api/zadania"
    },
    {
      "type": "STEP_BY_STEP",
      "heading": "Cały proces, lekcja po lekcji",
      "body": "1. (Lekcja 2) Wybierz PRZYPIĘTE wersje obrazów bazowych (`postgres:16`, `eclipse-temurin:21.0.2_13-...`) — nigdy `:latest`.\n2. (Lekcja 3) Napisz Dockerfile z odpowiednim `.dockerignore` (żeby nie skopiować `.git/`, `target/`, sekretów).\n3. (Lekcja 5) Uporządkuj instrukcje Dockerfile'a od najrzadziej do najczęściej zmieniających się (zależności przed kodem źródłowym).\n4. (Lekcja 9) Zastosuj multi-stage build — JDK+Maven w etapie `build`, TYLKO JRE w finalnym.\n5. (Lekcja 10) Dodaj `USER` — aplikacja NIGDY nie działa jako root.\n6. (Lekcja 8) Utwórz WŁASNĄ, nazwaną sieć — kontenery odnajdą się PO NAZWIE, nie po IP.\n7. (Lekcja 7) Utwórz NAZWANY wolumen dla bazy danych PRZED jej pierwszym uruchomieniem — dane PRZETRWAJĄ dowolne przyszłe zmiany kontenera.\n8. (Lekcja 4) Uruchom bazę danych — BEZ `-p` (niedostępna z hosta, tylko dla `tasks-api` na tej samej sieci).\n9. (Lekcja 6) Uruchom `tasks-api` z danymi logowania przekazanymi przez `-e`, WSKAZUJĄC na bazę PO NAZWIE KONTENERA (`baza`), NIE `localhost`.\n10. Zweryfikuj działanie CAŁEGO systemu przez `-p 8080:8080` z hosta.",
      "code": null
    },
    {
      "type": "NOTE",
      "heading": "Ten sam zestaw poleceń działa IDENTYCZNIE na KAŻDEJ maszynie",
      "body": "To jest DOKŁADNE rozwiązanie problemu z lekcji 1 ('u mnie działa'): KAŻDY członek zespołu, uruchamiając TĘ SAMĄ sekwencję poleceń (`docker network create`, `docker run` bazy, `docker build`, `docker run` aplikacji), dostaje IDENTYCZNIE działający system — niezależnie od tego, czy ma zainstalowaną Javę 17 czy 21, PostgreSQL 14 czy 16, na Windows, Mac czy Linux. Cała 'wiedza' o tym, JAK skonfigurować środowisko, jest ZAKODOWANA w Dockerfile'u i poleceniach `docker run` — NIE w ustnej instrukcji 'zainstaluj sobie X, potem Y, pamiętaj o Z'.",
      "code": null
    },
    {
      "type": "USAGE",
      "heading": "Naturalny kolejny krok — automatyzacja przez orkiestrację",
      "body": "Ręczne wpisywanie tej sekwencji poleceń (`network create`, `run` bazy, `build`, `run` aplikacji, w PRAWIDŁOWEJ kolejności) DZIAŁA, ale jest podatne na pomyłki i niewygodne przy WIELU kontenerach naraz. Narzędzia orkiestracji WIELOKONTENEROWEJ (jak Docker Compose) automatyzują DOKŁADNIE tę sekwencję z JEDNEGO pliku konfiguracyjnego — to ŚWIADOMIE OSOBNY temat (priorytet 'bardzo ważny', nie 'krytyczny', w raporcie rynkowym), więc POZA zakresem tego rozdziału — ale WSZYSTKO, czego ten kapston nauczył (obrazy, sieci, wolumeny, zmienne środowiskowe), jest DOKŁADNIE tym, co taki plik orkiestracji i tak konfiguruje 'pod maską'.",
      "code": null
    },
    {
      "type": "PITFALL",
      "heading": "Uruchomienie aplikacji ZANIM baza danych jest FAKTYCZNIE gotowa",
      "body": "'Kontener DZIAŁA' (`docker ps` pokazuje status 'Up') NIE oznacza 'usługa WEWNĄTRZ jest już GOTOWA przyjmować połączenia' — PostgreSQL POTRZEBUJE kilku sekund na inicjalizację PO starcie kontenera, ZANIM faktycznie zacznie nasłuchiwać na porcie 5432. Jeśli `tasks-api` wystartuje I OD RAZU spróbuje się połączyć (typowe dla aplikacji Spring Boot łączącej się z bazą PRZY STARCIE), może NAPOTKAĆ bazę, która TECHNICZNIE 'działa' (kontener uruchomiony), ale JESZCZE nie przyjmuje połączeń — kończąc się błędem POŁĄCZENIA, mimo że OBA kontenery, sieć i wolumen są skonfigurowane BEZ ŻADNEGO błędu.",
      "code": null
    },
    {
      "type": "CODE_WRONG",
      "heading": "Uruchomienie obu kontenerów 'naraz', bez uwzględnienia gotowości bazy",
      "body": "Nawet POPRAWNIE skonfigurowana sieć i wolumen NIE ROZWIĄZUJĄ problemu KOLEJNOŚCI GOTOWOŚCI — `tasks-api` MOŻE wystartować SZYBCIEJ niż PostgreSQL zdąży się w pełni zainicjalizować.",
      "code": "docker run -d --name baza --network javaquest-net -v pgdata:/var/lib/postgresql/data \\\n  -e POSTGRES_PASSWORD=haslo postgres:16\ndocker run -d --name tasks-api --network javaquest-net -p 8080:8080 \\\n  -e SPRING_DATASOURCE_URL=jdbc:postgresql://baza:5432/javaquest \\\n  javaquest-tasks-api:1.0\n# BLEDNIE: brak GWARANCJI, ze 'baza' jest GOTOWA na polaczenia, zanim 'tasks-api' probuje\n# sie polaczyc - Spring Boot moze rzucic blad polaczenia i ZAKONCZYC dzialanie PRZED\n# tym, jak PostgreSQL zdazy w pelni wystartowac (typowo kilka sekund)"
    },
    {
      "type": "CODE_RIGHT",
      "heading": "`HEALTHCHECK` (lekcja 10) jako sygnał FAKTYCZNEJ gotowości",
      "body": "Zamiast zgadywać, ile poczekać, `HEALTHCHECK` w obrazie bazy danych pozwala SPRAWDZIĆ (przez `docker inspect`/skrypt startowy) DOKŁADNY moment, w którym baza jest NAPRAWDĘ gotowa — i DOPIERO WTEDY uruchomić aplikację (albo skonfigurować w niej ponawianie połączenia, co Spring Boot wspiera natywnie).",
      "code": "docker run -d --name baza --network javaquest-net -v pgdata:/var/lib/postgresql/data \\\n  --health-cmd=\"pg_isready -U postgres\" --health-interval=2s \\\n  -e POSTGRES_PASSWORD=haslo postgres:16\n\n# skrypt startowy CZEKA na status 'healthy' PRZED uruchomieniem aplikacji:\nuntil [ \"$(docker inspect -f '{{.State.Health.Status}}' baza)\" = \"healthy\" ]; do sleep 1; done\n\ndocker run -d --name tasks-api --network javaquest-net -p 8080:8080 \\\n  -e SPRING_DATASOURCE_URL=jdbc:postgresql://baza:5432/javaquest \\\n  javaquest-tasks-api:1.0\n# TERAZ 'tasks-api' startuje DOPIERO gdy 'baza' faktycznie przyjmuje polaczenia"
    },
    {
      "type": "WHEN_TO_USE",
      "heading": "Mapa całego rozdziału — który krok kapstonu użył której lekcji",
      "body": "Wybór wersji obrazów bazowych -> lekcja 2. Napisanie Dockerfile'a + `.dockerignore` -> lekcja 3. Kolejność instrukcji dla cache'a -> lekcja 5. Konfiguracja przez zmienne środowiskowe -> lekcja 6. Trwałość danych bazy -> lekcja 7 (wolumen). Komunikacja między kontenerami -> lekcja 8 (sieć). Eliminacja narzędzi budowania z obrazu finalnego -> lekcja 9 (multi-stage). Nieuprzywilejowany użytkownik -> lekcja 10. Mapowanie portu dla dostępu z hosta, zarządzanie cyklem życia kontenerów -> lekcja 4. Zrozumienie SAMEJ izolacji i efemeryczności, która motywowała wszystko powyżej -> lekcja 1.",
      "code": null
    },
    {
      "type": "SUMMARY",
      "heading": "Podsumowanie całego rozdziału",
      "body": "Ten rozdział uzupełnił jedyny temat priorytetu 'critical' w raporcie rynkowym, który MIAŁ przypisany rozdział, ale z WYRAŹNĄ notatką o niepełnym pokryciu (Docker uczony TYLKO w kontekście konteneryzacji mikroserwisów) — teraz Docker ma PEŁNE, samodzielne wprowadzenie: kontenery kontra VM, obrazy i tagi, Dockerfile, uruchamianie, warstwy/cache, konfiguracja, wolumeny, sieci, multi-stage, bezpieczeństwo. Kapston pokazał, że te 10 elementów RZADKO występuje w izolacji — prawdziwe wdrożenie (jak dwukontenerowy JavaQuest Tasks API + PostgreSQL) wymaga ich WSZYSTKICH naraz, we WŁAŚCIWEJ KOLEJNOŚCI, z UWAGĄ na subtelności takie jak faktyczna gotowość usługi — dokładnie ta całościowa, praktyczna biegłość, o którą pyta prawdziwa rekrutacja Java/Spring wymieniająca Dockera jako 'częste' wymaganie priorytetu 'krytyczny'.",
      "code": null
    }
  ],
  "exercises": [
    {
      "prompt": "Napisz (jako String) pełną, poprawną sekwencję poleceń potrzebną do uruchomienia systemu z tego kapstonu od zera: utworzenie sieci, wolumenu, uruchomienie bazy danych, zbudowanie obrazu aplikacji, uruchomienie aplikacji - w prawidłowej kolejności.",
      "hint": "Skorzystaj z sekwencji w bloku code_practical, pamietajac o kolejnosci: siec i wolumen przed kontenerami, baza danych przed aplikacja.",
      "solution": "Poprawna sekwencja: (1) `docker network create javaquest-net` - sieć musi istnieć, zanim jakikolwiek kontener się do niej podłączy. (2) `docker volume create pgdata` - wolumen gotowy PRZED uruchomieniem bazy, żeby dane od razu trafiały do trwałego miejsca. (3) `docker run -d --name baza --network javaquest-net -v pgdata:/var/lib/postgresql/data -e POSTGRES_PASSWORD=haslo -e POSTGRES_DB=javaquest postgres:16` - baza PRZED aplikacją. (4) `docker build -t javaquest-tasks-api:1.0 .` - zbuduj obraz aplikacji (może się odbyć równolegle z krokami 1-3, bo nie zależy od nich). (5) `docker run -d --name tasks-api --network javaquest-net -p 8080:8080 -e SPRING_DATASOURCE_URL=jdbc:postgresql://baza:5432/javaquest -e SPRING_DATASOURCE_PASSWORD=haslo javaquest-tasks-api:1.0` - aplikacja NA KOŃCU, gdy baza już (najlepiej: potwierdzone jako gotowa) działa."
    },
    {
      "prompt": "Wyjaśnij (jako String), dlaczego poprawnie skonfigurowana sieć (lekcja 8) i wolumen (lekcja 7) nie wystarczą, żeby zagwarantować, że `tasks-api` połączy się z bazą danych bez błędu przy pierwszym uruchomieniu.",
      "hint": "Rozroznij dwa rozne pojecia: 'kontener dziala' (docker ps pokazuje status Up) kontra 'usluga wewnatrz kontenera jest gotowa przyjmowac polaczenia' - czy sa to zawsze te same momenty w czasie.",
      "solution": "Sieć i wolumen rozwiązują problemy ODNAJDYWANIA kontenerów (po nazwie) i TRWAŁOŚCI danych - ale NIE gwarantują, że usługa WEWNĄTRZ kontenera jest już GOTOWA do obsługi połączeń w momencie, gdy `docker ps` pokazuje jej status jako 'Up'. PostgreSQL (i wiele innych usług) POTRZEBUJE dodatkowego czasu PO starcie kontenera na WŁASNĄ, wewnętrzną inicjalizację (np. przygotowanie plików bazy danych), zanim FAKTYCZNIE zacznie nasłuchiwać na swoim porcie. Jeśli `tasks-api` próbuje połączyć się ZBYT WCZEŚNIE (kontener bazy już 'działa', ale usługa PostgreSQL wewnątrz jeszcze się inicjalizuje), połączenie zawiedzie, mimo że sieć i wolumen są skonfigurowane całkowicie poprawnie - to zupełnie INNY rodzaj problemu (kolejność GOTOWOŚCI w czasie), nierozwiązywany przez żadną z tych dwóch konfiguracji z osobna."
    },
    {
      "prompt": "Wyjaśnij (jako String), w jaki sposób `healthcheck` (wprowadzony w lekcji 10) pomaga rozwiązać problem z poprzedniego ćwiczenia - co dokładnie pozwala sprawdzić, czego nie sprawdza sam status 'kontener działa'?",
      "hint": "Przypomnij sobie z bloku code_right, jakie konkretnie polecenie jest uruchamiane przez healthcheck w tym przykladzie (pg_isready), i co ono faktycznie weryfikuje - stan samego kontenera, czy stan uslugi wewnatrz niego.",
      "solution": "`HEALTHCHECK` definiuje POLECENIE (tu: `pg_isready -U postgres`), które Docker uruchamia CYKLICZNIE WEWNĄTRZ kontenera, żeby sprawdzić, czy USŁUGA (nie sam kontener, ale KONKRETNIE proces PostgreSQL) faktycznie odpowiada i jest gotowa do pracy - to zupełnie inna, GŁĘBSZA weryfikacja niż sam status 'kontener działa' (który mówi tylko, że PROCES GŁÓWNY kontenera w ogóle wystartował, nie że jest już FUNKCJONALNIE gotowy). Status `healthy` (widoczny przez `docker inspect`) pojawia się DOPIERO, gdy `pg_isready` faktycznie zwróci sukces - to pozwala skryptowi startowemu CZEKAĆ na TEN KONKRETNY, wiarygodny sygnał gotowości, zamiast zgadywać, ile sekund 'powinno wystarczyć'."
    },
    {
      "prompt": "Wyjaśnij (jako String), dlaczego skonteneryzowanie całego systemu (aplikacja + baza danych) tak, jak w tym kapstonie, rozwiązuje dokładnie ten sam problem, który zmotywował cały rozdział w lekcji 1 ('u mnie działa') - odwołaj się konkretnie do tego, co różni się (albo nie różni) między maszynami różnych członków zespołu po zastosowaniu tego podejścia.",
      "hint": "Zastanow sie, co dokladnie kazdy czlonek zespolu musi miec zainstalowane na swoim hoscie, zeby uruchomic ten caly system - czy to Java, PostgreSQL w konkretnej wersji, czy tylko sam Docker.",
      "solution": "Po skonteneryzowaniu całego systemu, KAŻDY członek zespołu potrzebuje na swoim hoście WYŁĄCZNIE zainstalowanego Dockera - NIE musi mieć zainstalowanej konkretnej wersji Javy, PostgreSQL, ani żadnych innych zależności aplikacji BEZPOŚREDNIO na swoim systemie. CAŁA reszta (dokładna wersja JRE, dokładna wersja PostgreSQL, konfiguracja sieciowa, sposób budowania) jest ZAKODOWANA w Dockerfile'u i poleceniach `docker run`, IDENTYCZNA dla każdego, kto je uruchomi - eliminuje to DOKŁADNIE tę klasę problemów z lekcji 1 ('u mnie zainstalowana jest inna wersja X, więc u mnie działa inaczej'), bo NIKT już nie instaluje niczego RĘCZNIE, BEZPOŚREDNIO na swoim systemie - wszystko dzieje się WEWNĄTRZ identycznych, przenośnych kontenerów."
    },
    {
      "prompt": "Napisz (jako String) skrypt powłoki `start.sh`, który od zera uruchamia system z kapstonu: tworzy sieć i wolumen tylko jeśli nie istnieją, uruchamia bazę z healthcheckiem, czeka (maksymalnie 60 s) na status `healthy`, buduje obraz aplikacji i uruchamia ją. Skrypt ma kończyć się błędem, jeśli baza nie wstanie w czasie.",
      "hint": "docker network inspect / docker volume inspect zwracają błąd, gdy obiekt nie istnieje. Pętla z licznikiem i docker inspect -f '{{.State.Health.Status}}'.",
      "solution": "#!/bin/sh\nset -e\ndocker network inspect javaquest-net >/dev/null 2>&1 || docker network create javaquest-net\ndocker volume inspect pgdata >/dev/null 2>&1 || docker volume create pgdata\ndocker run -d --name baza --network javaquest-net -v pgdata:/var/lib/postgresql/data \\\n  -e POSTGRES_PASSWORD=\"$DB_PASSWORD\" -e POSTGRES_DB=javaquest \\\n  --health-cmd='pg_isready -U postgres -d javaquest' --health-interval=2s postgres:16\ni=0\nuntil [ \"$(docker inspect -f '{{.State.Health.Status}}' baza)\" = healthy ]; do\n  i=$((i+1)); [ $i -gt 30 ] && { echo 'Baza nie wstala'; docker logs --tail 20 baza; exit 1; }\n  sleep 2\ndone\ndocker build -t javaquest-tasks-api:1.0 .\ndocker run -d --name tasks-api --network javaquest-net -p 8080:8080 \\\n  -e SPRING_DATASOURCE_URL=jdbc:postgresql://baza:5432/javaquest \\\n  -e SPRING_DATASOURCE_PASSWORD=\"$DB_PASSWORD\" javaquest-tasks-api:1.0\n\nHasło przychodzi ze zmiennej środowiskowej powłoki (np. z pliku .env załadowanego wcześniej), a nie z treści skryptu."
    },
    {
      "prompt": "Opisz (jako String) procedurę aktualizacji aplikacji z `javaquest-tasks-api:1.0` do `1.1` w systemie z kapstonu, tak żeby baza i dane pozostały nietknięte, oraz jak wycofać wdrożenie, jeśli 1.1 nie przechodzi healthchecku.",
      "hint": "Kontener aplikacji jest wymienny - dane żyją w wolumenie bazy. Stary obraz nadal jest lokalnie dostępny.",
      "solution": "docker build -t javaquest-tasks-api:1.1 .\ndocker stop tasks-api && docker rm tasks-api\ndocker run -d --name tasks-api --network javaquest-net -p 8080:8080 \\\n  -e SPRING_DATASOURCE_URL=jdbc:postgresql://baza:5432/javaquest \\\n  -e SPRING_DATASOURCE_PASSWORD=\"$DB_PASSWORD\" javaquest-tasks-api:1.1\ncurl -f localhost:8080/actuator/health\n\nWycofanie: stop/rm kontenera 1.1 i ponowne uruchomienie z tagiem 1.0 (obraz nadal jest lokalnie; dlatego nie nadpisuje się tagów i nie używa latest). Baza nie jest dotykana. Uwaga: jeśli 1.1 wykonała migrację schematu bazy (Flyway), powrót do 1.0 może wymagać kompatybilnej wstecz migracji - zmiany schematu planuje się tak, żeby stara wersja nadal działała. Krótka przerwa przy wymianie kontenera to ograniczenie ręcznego podejścia - orkiestratory robią rolling update."
    },
    {
      "prompt": "Nowa osoba w zespole uruchomiła kapston i `tasks-api` kończy się po kilku sekundach. `docker logs tasks-api` pokazuje `org.postgresql.util.PSQLException: Connection to localhost:5432 refused`. Przeprowadź (jako String) diagnozę krok po kroku i wskaż, które polecenie z kapstonu zostało najpewniej zmienione.",
      "hint": "Aplikacja łączy się z localhost - czyli nie dostała zmiennej SPRING_DATASOURCE_URL albo ktoś ją zmienił.",
      "solution": "1) Komunikat mówi o localhost:5432 - aplikacja w kontenerze szuka bazy we własnym kontenerze. 2) `docker inspect --format '{{json .Config.Env}}' tasks-api` - czy jest SPRING_DATASOURCE_URL z hostem 'baza'? Najpewniej brakuje tej zmiennej (Spring użył domyślnej wartości z application.properties z localhost) albo ktoś wpisał localhost. 3) `docker inspect --format '{{json .NetworkSettings.Networks}}' tasks-api` - czy kontener jest w javaquest-net. 4) `docker ps` / `docker inspect -f '{{.State.Health.Status}}' baza` - czy baza działa i jest healthy. Poprawka: odtworzyć tasks-api z -e SPRING_DATASOURCE_URL=jdbc:postgresql://baza:5432/javaquest i --network javaquest-net."
    },
    {
      "prompt": "Porównaj (jako String) ręczne polecenia `docker` z kapstonu z tym, co da Docker Compose w kolejnym rozdziale: wypisz 5 problemów ręcznego podejścia (np. kolejność startu, powtarzalność konfiguracji, sprzątanie) i przy każdym wskaż, który mechanizm Compose go rozwiązuje.",
      "hint": "Compose to deklaratywny plik YAML opisujący usługi, sieci i wolumeny + jedno polecenie up/down.",
      "solution": "1) Długie polecenia z wieloma flagami, łatwe do pomylenia -> deklaratywny plik compose.yaml w repozytorium.\n2) Ręczne czekanie na gotowość bazy -> depends_on z condition: service_healthy.\n3) Ręczne tworzenie sieci i wolumenów -> Compose tworzy je automatycznie (sieć projektu, nazwane wolumeny).\n4) Sprzątanie wielu obiektów -> docker compose down (opcjonalnie -v dla wolumenów).\n5) Konfiguracja w historii powłoki -> env_file / .env i zmienne w pliku, wersjonowane (bez sekretów).\n(+ budowanie obrazu: build: . w definicji usługi; skalowanie: --scale.)"
    },
    {
      "prompt": "Przygotuj (jako String) checklistę 'gotowe do przekazania zespołowi' dla skonteneryzowanej aplikacji z kapstonu - co najmniej 8 punktów, z konkretnym poleceniem lub plikiem przy każdym, obejmujących obraz, bezpieczeństwo, dane, konfigurację i dokumentację.",
      "hint": "Zbierz praktyki z lekcji 2-10: tagi, .dockerignore, multi-stage, USER, HEALTHCHECK, wolumen, sieć, sekrety, skan.",
      "solution": "1) Przypięte wersje obrazów (FROM eclipse-temurin:21.0.x-jre, postgres:16.x).\n2) .dockerignore (bez .git, .env, target).\n3) Multi-stage build - obraz końcowy tylko z JRE i JAR-em (docker images - rozmiar ~250 MB).\n4) USER nie-root + COPY --chown.\n5) ENTRYPOINT w formie exec (graceful shutdown).\n6) HEALTHCHECK aplikacji i bazy (docker ps pokazuje healthy).\n7) Wolumen nazwany dla danych bazy (docker volume ls).\n8) Własna sieć; baza bez -p (dostępna tylko dla aplikacji).\n9) Sekrety poza obrazem (.env.example w repo, prawdziwe wartości poza nim).\n10) Skan podatności (trivy image) bez CRITICAL.\n11) README: jak zbudować, uruchomić, zaktualizować i posprzątać."
    },
    {
      "prompt": "Wyjaśnij (jako String), dlaczego `docker rm -f baza` jest bezpieczne w systemie z kapstonu, a `docker volume rm pgdata` nie, i jak przed ryzykowną operacją (np. zmianą wersji głównej PostgreSQL) zabezpieczyć dane logiczną kopią. Podaj polecenia.",
      "hint": "Dane są w wolumenie, nie w kontenerze. pg_dump przez docker exec, przekierowanie na host.",
      "solution": "Kontener bazy to tylko proces z konfiguracją - dane są w wolumenie pgdata, więc usunięcie kontenera ich nie rusza (nowy kontener z -v pgdata:... je zobaczy). Usunięcie wolumenu kasuje dane nieodwracalnie.\nKopia logiczna przed ryzykowną operacją:\ndocker exec baza pg_dump -U postgres -d javaquest -Fc > javaquest-$(date +%F).dump\nPrzywrócenie (np. do nowej bazy na postgres:17):\ndocker exec -i baza-nowa pg_restore -U postgres -d javaquest < javaquest-2026-09-26.dump\nFormat -Fc (custom) jest skompresowany i pozwala na wybiórcze przywracanie; zwykły plik SQL (bez -Fc) przywraca się przez psql."
    }
  ],
  "quiz": [
    {
      "question": "W jakiej kolejności powinny zostać uruchomione elementy systemu z tego kapstonu?",
      "options": {
        "A": "Aplikacja, potem sieć, potem baza danych, potem wolumen",
        "B": "Sieć i wolumen jako pierwsze (infrastruktura), następnie baza danych, na końcu aplikacja",
        "C": "Kolejność nie ma żadnego znaczenia - Docker automatycznie ją optymalizuje",
        "D": "Zawsze naprzemiennie - jeden krok bazy danych, jeden krok aplikacji"
      },
      "correct": "B",
      "explanation": "Infrastruktura (sieć, wolumen) musi istnieć przed kontenerami, które jej używają, a baza danych powinna być gotowa przed aplikacją, która się z nią łączy przy starcie."
    },
    {
      "question": "Dlaczego status 'kontener działa' (docker ps pokazuje Up) nie gwarantuje, że usługa wewnątrz jest gotowa przyjmować połączenia?",
      "options": {
        "A": "To zawsze to samo - status Up oznacza pełną gotowość usługi",
        "B": "Usługa wewnątrz kontenera (np. PostgreSQL) może potrzebować dodatkowego czasu na własną inicjalizację po starcie procesu głównego, zanim faktycznie zacznie nasłuchiwać na porcie",
        "C": "Docker celowo opóźnia raportowanie statusu Up o kilka sekund",
        "D": "Status Up dotyczy wyłącznie kontenerów bez podłączonego wolumenu"
      },
      "correct": "B",
      "explanation": "Status kontenera (uruchomiony proces główny) i gotowość konkretnej usługi wewnątrz to dwie różne rzeczy - usługa może potrzebować dodatkowego czasu inicjalizacji, mimo że kontener już formalnie działa."
    },
    {
      "question": "Do czego służy healthcheck w kontekście problemu kolejności startowej?",
      "options": {
        "A": "Do automatycznego usuwania niezdrowych kontenerów",
        "B": "Do zdefiniowania polecenia sprawdzającego faktyczną gotowość usługi wewnątrz kontenera, pozwalającego czekać na wiarygodny sygnał 'healthy' zamiast zgadywać czas oczekiwania",
        "C": "Do automatycznego skalowania liczby kontenerów",
        "D": "Do szyfrowania komunikacji między kontenerami"
      },
      "correct": "B",
      "explanation": "Healthcheck pozwala precyzyjnie zweryfikować, czy usługa wewnątrz kontenera faktycznie odpowiada, dając wiarygodny sygnał gotowości zamiast arbitralnego czasu oczekiwania."
    },
    {
      "question": "Co jest jedyną rzeczą, jaką każdy członek zespołu musi mieć zainstalowaną na swoim hoście po pełnym skonteneryzowaniu systemu (aplikacja + baza danych)?",
      "options": {
        "A": "Dokładnie taką samą wersję Javy i PostgreSQL jak wszyscy pozostali",
        "B": "Wyłącznie Dockera - cała reszta (wersje, konfiguracja) jest zakodowana w Dockerfile'ach i poleceniach docker run",
        "C": "Osobną maszynę wirtualną dla każdego kontenera",
        "D": "Ręcznie skonfigurowane środowisko IDE zgodne z zespołowym standardem"
      },
      "correct": "B",
      "explanation": "Skonteneryzowanie całego systemu eliminuje potrzebę instalowania czegokolwiek poza samym Dockerem - wszystkie zależności i ich dokładne wersje są zamknięte wewnątrz obrazów."
    },
    {
      "question": "Dlaczego narzędzia orkiestracji wielokontenerowej (jak Docker Compose) są świadomie poza zakresem tego rozdziału?",
      "options": {
        "A": "Ponieważ w ogóle nie istnieją takie narzędzia",
        "B": "Ponieważ to osobny temat o niższym priorytecie w raporcie rynkowym ('bardzo ważny', nie 'krytyczny'), mimo że automatyzuje dokładnie to, czego ten rozdział uczy ręcznie",
        "C": "Ponieważ Docker Compose jest przestarzały i nieużywany w praktyce",
        "D": "Ponieważ wymaga zupełnie innej wiedzy niezwiązanej z tym rozdziałem"
      },
      "correct": "B",
      "explanation": "Docker Compose (i podobne narzędzia) automatyzują dokładnie tę sekwencję ręcznych poleceń nauczoną w tym rozdziale, ale jako osobny temat o niższym priorytecie w analizie rynku pracy, świadomie pozostawiony poza zakresem."
    },
    {
      "question": "W jakiej kolejności należy uruchamiać elementy systemu z kapstonu?",
      "code": "# sieć javaquest-net, wolumen pgdata, kontener baza, obraz aplikacji, kontener tasks-api",
      "options": {
        "A": "Sieć i wolumen, potem baza (i czekanie na healthy), potem build obrazu i tasks-api",
        "B": "tasks-api, baza, sieć, wolumen",
        "C": "Wszystko jednocześnie",
        "D": "Wolumen po uruchomieniu bazy"
      },
      "correct": "A",
      "explanation": "Kontener można podłączyć tylko do istniejącej sieci, a wolumen musi być podany przy tworzeniu kontenera bazy. Aplikacja startuje ostatnia - gdy baza faktycznie przyjmuje połączenia."
    },
    {
      "question": "Co oznacza ta pętla w skrypcie startowym?",
      "code": "until [ \"$(docker inspect -f '{{.State.Health.Status}}' baza)\" = \"healthy\" ]; do\n  sleep 1\ndone",
      "options": {
        "A": "Czeka, aż kontener baza się zatrzyma",
        "B": "Sprawdza, czy obraz istnieje",
        "C": "Restartuje bazę co sekundę",
        "D": "Czeka, aż healthcheck bazy (np. pg_isready) zacznie zwracać sukces - dopiero wtedy uruchamiana jest aplikacja"
      },
      "correct": "D",
      "explanation": "Status Up oznacza tylko działający proces. PostgreSQL potrzebuje kilku sekund na inicjalizację (przy pierwszym starcie dłużej). Warto dodać limit prób, żeby skrypt nie czekał w nieskończoność."
    },
    {
      "question": "Co sprawdza to polecenie healthchecku bazy?",
      "code": "--health-cmd=\"pg_isready -U postgres -d javaquest\"",
      "options": {
        "A": "Czy serwer PostgreSQL przyjmuje połączenia (narzędzie z obrazu postgres zwraca 0, gdy baza jest gotowa)",
        "B": "Czy baza ma kopię zapasową",
        "C": "Czy tabela javaquest ma dane",
        "D": "Czy port 5432 jest opublikowany na hoście"
      },
      "correct": "A",
      "explanation": "pg_isready jest w obrazie postgres, więc nie trzeba niczego doinstalowywać. Healthcheck powinien sprawdzać możliwie tanio to, czego potrzebują klienci usługi."
    },
    {
      "question": "Aplikacja kończy się błędem 'Connection to localhost:5432 refused'. Co jest najbardziej prawdopodobną przyczyną?",
      "code": "$ docker logs tasks-api\norg.postgresql.util.PSQLException: Connection to localhost:5432 refused.",
      "options": {
        "A": "Baza nie ma wolumenu",
        "B": "Brak -p 5432:5432 przy bazie",
        "C": "Zła wersja Javy",
        "D": "Kontener aplikacji nie dostał SPRING_DATASOURCE_URL z hostem 'baza' (użył domyślnego localhost) - localhost w kontenerze to sam kontener aplikacji"
      },
      "correct": "D",
      "explanation": "Diagnoza: docker inspect --format '{{json .Config.Env}}' tasks-api. Publikacja portu bazy na hoście nic by tu nie zmieniła - kontenery rozmawiają przez sieć Dockera."
    },
    {
      "question": "Co stanie się z danymi po tych poleceniach?",
      "code": "$ docker rm -f baza\n$ docker run -d --name baza --network javaquest-net -v pgdata:/var/lib/postgresql/data \\\n    -e POSTGRES_PASSWORD=haslo postgres:16",
      "options": {
        "A": "Zostaną - dane są w wolumenie pgdata, który podłączono do nowego kontenera",
        "B": "Znikną - baza jest nowa",
        "C": "Zostaną tylko tabele systemowe",
        "D": "Nowy kontener nie wystartuje"
      },
      "correct": "A",
      "explanation": "Kontener bazy jest wymienny, dane nie. Uwaga: przy istniejących danych zmienne POSTGRES_PASSWORD/POSTGRES_DB nie są ponownie stosowane - obraz inicjalizuje bazę tylko przy pustym katalogu danych."
    },
    {
      "question": "Dlaczego baza w kapstonie nie ma opcji -p?",
      "code": "$ docker run -d --name baza --network javaquest-net ... postgres:16      # bez -p\n$ docker run -d --name tasks-api --network javaquest-net -p 8080:8080 ...",
      "options": {
        "A": "To błąd - aplikacja się nie połączy",
        "B": "Bo postgres nie obsługuje -p",
        "C": "Bo aplikacja łączy się przez sieć Dockera, a baza nie musi (i ze względów bezpieczeństwa nie powinna) być dostępna z hosta ani sieci lokalnej",
        "D": "Bo port 5432 jest zawsze zajęty"
      },
      "correct": "C",
      "explanation": "Publikuje się tylko to, co ma być dostępne z zewnątrz. Do jednorazowego podejrzenia bazy można użyć docker exec -it baza psql zamiast publikować port."
    },
    {
      "question": "Jak wycofać nieudane wdrożenie wersji 1.1 aplikacji?",
      "code": "$ docker run -d --name tasks-api ... javaquest-tasks-api:1.1\n$ curl -f localhost:8080/actuator/health   # błąd",
      "options": {
        "A": "Przywrócić bazę z kopii",
        "B": "Nie da się bez przebudowania obrazu",
        "C": "docker rollback tasks-api",
        "D": "docker rm -f tasks-api, a potem docker run ... javaquest-tasks-api:1.0 (obraz z poprzednią wersją nadal jest dostępny pod swoim tagiem)"
      },
      "correct": "D",
      "explanation": "Niezmienne, wersjonowane tagi (zamiast nadpisywania latest) sprawiają, że wycofanie to po prostu uruchomienie poprzedniego obrazu. Ostrożnie z migracjami schematu bazy wykonanymi przez nową wersję."
    },
    {
      "question": "Jak wykonać logiczną kopię bazy z kontenera do pliku na hoście?",
      "code": "$ docker exec baza pg_dump -U postgres -d javaquest -Fc ??? javaquest.dump",
      "options": {
        "A": ">",
        "B": "-o",
        "C": "--to-host",
        "D": "| docker cp"
      },
      "correct": "A",
      "explanation": "Wyjście pg_dump (stdout procesu w kontenerze) trafia przez docker exec do terminala hosta, a przekierowanie '>' zapisuje je do pliku. Przywracanie: docker exec -i baza pg_restore ... < plik."
    },
    {
      "question": "Które polecenie usunie wszystkie elementy systemu z kapstonu łącznie z danymi?",
      "code": "# kontenery: tasks-api, baza | sieć: javaquest-net | wolumen: pgdata",
      "options": {
        "A": "docker rm -f tasks-api baza",
        "B": "docker system prune",
        "C": "docker rm -f tasks-api baza && docker network rm javaquest-net && docker volume rm pgdata",
        "D": "docker stop baza"
      },
      "correct": "C",
      "explanation": "Każdy typ obiektu usuwa się osobno, a wolumen - jawnie (to zabezpieczenie danych). Docker Compose skraca to do jednego polecenia: docker compose down -v."
    },
    {
      "question": "Dlaczego w kapstonie hasło bazy powinno pochodzić ze zmiennej środowiskowej powłoki, a nie być wpisane w skrypt start.sh?",
      "code": "# (1) -e POSTGRES_PASSWORD=haslo123           w start.sh\n# (2) -e POSTGRES_PASSWORD=\"$DB_PASSWORD\"      w start.sh",
      "options": {
        "A": "Bo (1) nie zadziała",
        "B": "Bo Docker wymaga cudzysłowów",
        "C": "Bo skrypt trafia do repozytorium - (1) zapisałoby hasło w historii Gita; w (2) wartość przychodzi z lokalnego środowiska (np. z .env poza repozytorium)",
        "D": "Nie ma różnicy"
      },
      "correct": "C",
      "explanation": "Zasada z rozdziału o Gicie (lekcja 12) obowiązuje także dla skryptów wdrożeniowych: sekrety poza repozytorium, a w repozytorium tylko ich nazwy."
    },
    {
      "question": "Jak ustalić, czy obraz aplikacji z kapstonu spełnia zasadę 'nie-root'?",
      "code": "$ docker run --rm --entrypoint whoami javaquest-tasks-api:1.0\n$ docker inspect --format '{{.Config.User}}' javaquest-tasks-api:1.0",
      "options": {
        "A": "Nie da się sprawdzić bez kodu źródłowego",
        "B": "Trzeba sprawdzić logi bazy",
        "C": "Wystarczy docker ps",
        "D": "whoami powinno wypisać nazwę użytkownika aplikacji, a Config.User - wartość z instrukcji USER (pusta oznacza root)"
      },
      "correct": "D",
      "explanation": "Takie sprawdzenie łatwo zautomatyzować w CI (test obrazu), żeby przypadkowe usunięcie USER z Dockerfile nie przeszło niezauważone."
    },
    {
      "question": "Co się stanie przy pierwszym starcie postgres z nowym, pustym wolumenem i zmienną POSTGRES_DB=javaquest?",
      "code": "$ docker run -d --name baza -v pgdata:/var/lib/postgresql/data \\\n    -e POSTGRES_PASSWORD=haslo -e POSTGRES_DB=javaquest postgres:16",
      "options": {
        "A": "Skrypt startowy obrazu zainicjalizuje katalog danych, ustawi hasło i utworzy bazę javaquest (tylko przy pustym katalogu danych)",
        "B": "Nic - bazę trzeba utworzyć ręcznie",
        "C": "Baza zostanie utworzona przy każdym starcie od nowa",
        "D": "Błąd - brak pliku SQL"
      },
      "correct": "A",
      "explanation": "Inicjalizacja odbywa się tylko raz. Zmiana POSTGRES_PASSWORD przy kolejnym uruchomieniu na istniejących danych NIE zmienia hasła - to częste źródło 'password authentication failed'."
    },
    {
      "question": "Który element kapstonu rozwiązuje problem 'aplikacja startuje, zanim baza jest gotowa'?",
      "code": "# sieć | wolumen | healthcheck bazy + czekanie na healthy | multi-stage build",
      "options": {
        "A": "Sieć",
        "B": "Wolumen",
        "C": "Healthcheck bazy i czekanie na status healthy przed uruchomieniem aplikacji",
        "D": "Multi-stage build"
      },
      "correct": "C",
      "explanation": "Sieć i wolumen zapewniają łączność i trwałość, ale nie kolejność. Dodatkowo warto, żeby sama aplikacja umiała ponawiać połączenie (np. pula HikariCP przy chwilowych przerwach)."
    },
    {
      "question": "Czego potrzebuje nowy członek zespołu na swoim komputerze, żeby uruchomić cały system z kapstonu?",
      "code": "# aplikacja Spring Boot (Java 21, Maven) + PostgreSQL 16",
      "options": {
        "A": "JDK 21, Mavena i PostgreSQL 16",
        "B": "Tylko JDK",
        "C": "Tylko Dockera (oraz kodu z repozytorium) - JDK i Maven są w etapie build, a PostgreSQL w obrazie",
        "D": "Maszynę wirtualną z Linuksem"
      },
      "correct": "C",
      "explanation": "To odpowiedź na problem 'u mnie działa' z lekcji 1: wersje narzędzi są zapisane w Dockerfile i tagach obrazów, a nie w instrukcji instalacji."
    },
    {
      "question": "Jak podejrzeć ostatnie wyniki healthchecku bazy, gdy status to unhealthy?",
      "code": "$ docker inspect --format '???' baza",
      "options": {
        "A": "{{.State.Status}}",
        "B": "{{.Health.Log}}",
        "C": "{{.Config.Healthcheck}}",
        "D": "{{json .State.Health}}"
      },
      "correct": "D",
      "explanation": "State.Health zawiera stan, licznik kolejnych porażek i dziennik ostatnich prób z kodem wyjścia i wyjściem polecenia - zwykle od razu widać przyczynę (np. złą nazwę bazy w pg_isready)."
    },
    {
      "question": "Dlaczego obraz aplikacji w kapstonie ma tag 1.0 zamiast latest?",
      "code": "$ docker build -t javaquest-tasks-api:1.0 .",
      "options": {
        "A": "Bo wersjonowany tag jednoznacznie wskazuje, co działa, i pozwala wrócić do poprzedniej wersji; latest jest nadpisywany przy każdym buildzie",
        "B": "Bo latest jest zarezerwowany dla Docker Hub",
        "C": "Bo Docker nie pozwala budować z latest",
        "D": "Nie ma to znaczenia"
      },
      "correct": "A",
      "explanation": "Te same argumenty co przy obrazach bazowych (lekcja 2) dotyczą Twoich własnych obrazów: powtarzalność i możliwość wycofania."
    },
    {
      "question": "Który zestaw poleceń najlepiej diagnozuje, czemu tasks-api nie widzi bazy?",
      "code": "# objaw: UnknownHostException: baza",
      "options": {
        "A": "docker images; docker system df",
        "B": "docker volume ls",
        "C": "docker network inspect javaquest-net; docker inspect --format '{{json .NetworkSettings.Networks}}' tasks-api baza",
        "D": "docker history javaquest-tasks-api:1.0"
      },
      "correct": "C",
      "explanation": "UnknownHostException oznacza problem z rozwiązywaniem nazwy - najczęściej kontenery są w różnych sieciach albo baza nie istnieje pod tą nazwą. Sprawdza się więc przynależność do sieci."
    },
    {
      "question": "Co najlepiej opisuje rolę Docker Compose względem kapstonu?",
      "code": "# kapston: ~6 ręcznych poleceń docker + skrypt czekający na healthy",
      "options": {
        "A": "Compose zastępuje Dockera",
        "B": "Compose służy do budowania JAR-ów",
        "C": "Compose działa tylko w chmurze",
        "D": "Compose opisuje ten sam system deklaratywnie w pliku YAML (usługi, sieć, wolumeny, zależności ze zdrowiem) i uruchamia go jednym poleceniem"
      },
      "correct": "D",
      "explanation": "Pod spodem Compose używa tych samych mechanizmów, które poznałeś w tym rozdziale - sieci, wolumenów, zmiennych i healthchecków. Rozumienie ich ułatwia debugowanie Compose."
    },
    {
      "question": "Który problem pozostaje przy ręcznej wymianie kontenera aplikacji (stop -> rm -> run nowej wersji)?",
      "code": "$ docker stop tasks-api && docker rm tasks-api\n$ docker run -d --name tasks-api ... javaquest-tasks-api:1.1",
      "options": {
        "A": "Utrata danych bazy",
        "B": "Krótka niedostępność usługi między zatrzymaniem starej a startem nowej wersji - rolling update bez przerwy zapewniają dopiero orkiestratory",
        "C": "Konieczność przebudowania bazy",
        "D": "Żaden"
      },
      "correct": "B",
      "explanation": "Przy jednym kontenerze nie da się uniknąć przerwy. Orkiestratory (Kubernetes, rozdział _36) uruchamiają nową wersję obok starej i przełączają ruch dopiero, gdy nowa jest gotowa."
    },
    {
      "question": "Hasło do bazy zmieniono w skrypcie, ale aplikacja nadal dostaje 'password authentication failed', mimo że obie strony mają nową wartość. Co jest przyczyną?",
      "code": "$ docker rm -f baza\n$ docker run -d --name baza -v pgdata:/var/lib/postgresql/data \\n    -e POSTGRES_PASSWORD=noweHaslo postgres:16      # wolumen z danymi już istnieje",
      "options": {
        "A": "Obraz postgres ignoruje zmienne środowiskowe",
        "B": "Aplikacja cache'uje hasło",
        "C": "POSTGRES_PASSWORD jest stosowane tylko przy inicjalizacji pustego katalogu danych - w istniejącej bazie hasło trzeba zmienić poleceniem ALTER USER",
        "D": "Trzeba usunąć sieć"
      },
      "correct": "C",
      "explanation": "Skrypty inicjalizacyjne obrazu działają tylko przy pierwszym starcie z pustym wolumenem. Zmiana hasła w istniejącej bazie: docker exec -it baza psql -U postgres -c \"ALTER USER postgres PASSWORD '...';\"."
    }
  ]
}
